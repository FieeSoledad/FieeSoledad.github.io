
---

title: mit-6.5840-lab3分析与实现

description: 实现mit-6.5840-lab3实现过程中的思考与踩坑。

published: 2025-12-14

category: 分布式

tags: [分布式, Golang, Raft]

---

```
本实验中，我将构建一个容错的键值存储系统

实现raft，一个复制状态机协议

把服务分片到多个复制状态机上，以实现更高的性能



挑战在于复制可能导致副本保存的数据不一致



raft将客户端请求组织成一个日志序列，并确保所有副本服务器看见相同的日志序列



只要多数派存活系统就能持续运行



本实验中，raft接口将支持一个无限序列的带编号的命令，条目通过索引号进行编号。日志条目被提交后，raft应该将日志条目发送给上层服务执行。



你将实现大部分内容，包括持久化状态保存以及故障节点重启后读取状态，但是没有集群成员变更。



实验分为四部分提交

LeaderElection

Log

Persist

LogCompaction





-race有什么用

键值服务器必须支持的接口

Make

start，就新的日志条目达成共识

GetState，查询当前任期，以及它是否认为自己是领导者

每当有新条目被提交到日志中，每个raft节点都向服务发送一条ApplyMsg，将日志应用于状态





3A

领导者选举

实现领导者选举和心跳

成功选举出单一领导者

将图2中领导者选举的状态添加到raft结构体中，需要定义结构体来保存每个日志条目的信息



一个goroutine定期发送RequestVote触发领导者选举

发送心跳包的频率不得超过每秒10次

5s内选举出新的领导者



调试技巧，可视化器



3B

日志

实现领导者和追随者追加日志的代码

日志索引是从1开始的，但是建议从0开始，

首先实现start函数，然后根据图2编写AppendEntries rpc发送请求



实现选举限制：

不要不断循环检查某些事件，使用sleep或者条件变量



如果运行过慢，会导致测试失败



如果总测试超过1min，或者我的cpu时间远超5s，那么后面可能会有问题





3C

持久化

本实现不会直接使用磁盘，而是Persister，使用其读状态和持久化状态

将状态序列化为一个字节数组



回溯不止一个条目是什么意思



还有循环执行同一个指令的方法呀，真的很有意思呢。



3D

日志压缩

永远记住完成的raft日志是不现实的，



实验中，测试程序会定期调用Snapshot，



一个完成的分布式系统

还有所谓的InstallSnapshot，直接使用整个状态复制，避免导致落后太多的副本服务器跟不上进度。



持久化应该连快照一起持久化









现在来看看raft的结构吧

Q：raft抽象解析

定义了必须暴露给服务器的API

成员

mu互斥锁

peers，客户端数组

persister，持久化对象

me，当前成员的编号

dead，是否死亡，为什么要用4字节

封装行为

获取状态，任期以及是否是领导者

emmm，很多东西都不是很清楚呢





Q：看看这个rfsvr的抽象

ts，测试上下文

me，当前服务器索引？

applyErr，

lastApplied

persister，测试中的持久化对

mu互斥锁

raft，服务器通过一个接口持有raft

logs，日志什么的呢。





Q：看看ApplyMsg抽象

成员

CommandValid，指令是否有效

Command，指令，为什么是一个接口

CommandIndex，指令索引

SnapshotValid，是否是快照

Snapshot，快照字节

SnapshotTerm，快照任期

SnapshotTerm，快照最后一个索引







Q；为什么election timeout时间要远高于heartbeat interval发送的时间间隔？

A：选举超市触发是一个定时任务，需要heartbeat来抑制，心跳包时间间隔远小是考虑有网络丢包的可能导致不必要的选举



Q：还需要定义一个结构体来保存每个日志条目的信息

A：这个是什么意思？





本实验的测试用例要求heartbeat间隔大于100ms，因此election timeout需要更大了



整理清楚选举逻辑：

1任期自增

2自己进入候选

3自己给自己投一票

4发送请求选举

5重置自己的选举定时器

6等待回复





先考虑commitIndex如何成为集群共识，再考虑新选举的Leader如何回滚日志





Q：请求投票时候，如果发现当前服务器已经落后了，自己是进入Follower，防止任期爆炸？

A：明白写了convert to follower



Q：commitIndex，lastApplied有什么用？

A；lastApplied：状态机应用日志到哪了，为什么要分离提交和应用？



Q：一条日志什么时候认为是提交的？大多数服务器表示已经应用到state中的。

A：lastApplied就是副本服务器的进度是吗





Q；状态nextIndex和matchIndex都是数组，什么用？

A：

nextIndex这些只有Leader会使用，就是Leader记忆各个Follower日志条目补充到了哪里

matchIndex是记录每个Follower跟到哪里了，用来计算大多数究竟能否提交。



Q；prevLogIndex和prevLogTerm变量是做什么的呢

A：

这两个字段是实现日志一致性的指纹，就是问你上个日志是不是我认为的那样，如果不是，就不要接受我的日志了

否则可能出现覆盖已提交日志，日志空洞，脑裂等灾难问题。





Q：看看AppendEntries抽象

A：作用：用于将命令追加到副本服务器



参数：

term，leader的任期

leaderId，这样follower知道leader是谁，通过哪个客户端回复

prevLogIndex，

prevLogTerm

entries，多个日志条目

leaderCommit，leader的提交日志索引



返回：

term：currentTerm，让leader自己更新，

success：



Q：总而言之，思考方式是Leader每个rpc请求都要考虑自己的心跳包没能到达导致选举被触发的可能性。

A：就是这样啊



Q：什么时候可能产生需要日志回滚的情况？

A：很简单，因为Leader发送给副本服务器的command未必就是提交的，如果此时Leader挂掉，新选举出的Leader可能就要回滚副本服务器的日志了



Q：那么提交的可靠性究竟如何保证呢？Leader如何通知副本服务器更新commitIndex呢？

A：commitIndex的共识如何达成呢？

Leader检查到某个index的日志在matchIndex中超过半数了，自己更新commitIndex，可以给客户端回复了，但是如何升级Leader当前commitIndex成为集群共识？

首先猜想是给副本服务器发appendentries，携带这个commitIndex

grok说推进commitIndex就可以立即回复客户端了

说是有一条铁律就是只要一条日志被大多数副本服务器append，新的Leader也不会撤销这一条log？





Q：为什么新的Leader不会撤销？

A：很简单，如果是一个没有跟进到最新Log的副本服务器发起选举，会由于自己的lastLog不够新被大多数服务器否决，永远无法获得多数选票。



Q：这么看来新的Leader的commitIndex很难从上一任Leader直接继承，需要某种计算机制

A：

先看看新选举的Leader有什么状态可以依赖用于计算commitIndex

currentTerm，没用

log，持久化内容

只有这两个持久化状态

1直接取log中最新的日志作为commitIndex可以吗

可能最新日志其实是leader追加的还未达成集群共识的日志条目，所以应该是不行的呢。

所以应该有一种回滚机制

在appendentries中的日志指纹，原来就是一个个试一边呀。



这里一个个尝试，除了保证多数副本服务器复制即可



Q：appendentries两个返回值如何给新的Leader提供日志复制进度呢？

A：答案就是什么呢我操死你的吗





Q：有没有可能一个原本落后的副本服务器term跑得很快？

A：term跑得快没用，还得日志进度跟上才能获得选票





Q：log中日志条目需要哪些字段

index和任期



Q：还有问题就是为什么拉投票的时候发现某个服务器任期更高，一定要直接进入Follower吗？

A：不一定，但是要放弃选举，更新任期，重置选举定时器，这样才不会一定被拒绝，浪费网络资源，加速集群收敛



Q：nextIndex和matchIndex都是记录副本服务器的复制进度，信息是不是冗余了





内核精准唤醒阻塞的线程：定时器本质原理实现





Q：lastLogIndex，lastLogTerm









Q：确保日志足够新，为什么需要最后日志的任期和索引同时保证？能不能只用索引保证？

A：其实应该也可以，因为如果新的Leader在版本号对不上的时候是没办法往副本服务器追加日志的吧。





Q：同一个任期内有无可能一个candidate向一个server发送多个requestVote？

A：有可能，因为是不可靠网络。



Q：会存在有的副本服务器一直断联吧？那么拉选票时一定要等到明确答复吗？

A：不用，因为试试其他的可能很快凑齐半数服务器。一直重试则不一定









看看Leader选举返回的日志索引到底是什么意思





raft的选举超时时间被设置为1s钟





看看测试框架吧，原来如此





makeAnnotation创建一个注解





Q：看看Annotation如何抽象的

mu互斥锁

annotations，标记追踪线性一致性，一个切片

continuous，一个map

finalized，一个布尔值



Q：Continuous抽象

成员

start，int64

desp，字符串

details，细节

bgcolor，背景颜色





Leader选举逻辑测试：总之，MakeConfig，通过传入Test的mksrv闭包解决了服务器的初始化问题

直接检查是否

这里没有关于日志下标的事情





选举中，除了候选人与servers的拉票通信，还有被拉票通信，我确实考虑了拉票通信，但是没有考虑被拉票通信。

有一种拉票通信，就是我被人拉票，发现对方任期更高，那么如果我直接跳入对方的任期成为其follower即可

那么问题来了



Q：如果一个日志落后的服务器任期跑得快怎么办呢？

A：它无论如何都没有资格成为Leader呀，这个问题已解决，其他candidate看见Term那么高，直接一起更新就完事了





我是不是忘记了并发控制修改成员？





Q：拉票与被拉票是并行进行的，如果被更高任期的拉票了，如何中止当前任期的选举？

A：使用锁保证两者顺序执行？



Q：但是这样还是需要一种机制让低任期的Leader放弃Leader身份，也就是什么所谓的，可是单更改一个nodeState没用，需要触发重新选举诶。

A：那就用心跳机制，一旦放弃Leader身份，就停止心跳，这样很快就是触发election timeout





Q：统计一下系统的协程工作呢

A：

Leader：心跳/选举

Replication：选举

只有选举是何时都不能停的 







Q：哪些状态是需要保护的？哪些字段是竞争读写的？

A：我居然写出了跨服务器的死锁，真的牛皮



成员

dead

nodeState

curTerm

votedFor

这些是需要竞争读写的



如果

拉票start：可能选举成功，修改curTerm和nodeState之类的

被拉票requestVote，可能遇见高任期的，需要修改任期



但是拉票start的任期应该具有一种持续性，我认为我一直是在curTerm竞选Leader的，如果这个时候发现有人在什么呢，也不能直接context取消吧？





类似于双重检查锁的机制，缓存流程开始的状态，在后面修改时检查状态是否与预期的一致。这在cmu15445中的check_func也有用到诶，check_func是在做什么呢，标定







核心属性就是

任期

任期投票对象

当前角色，这些要一起读取一起写入，都使用乐观锁的机制：假定没有问题，执行完流程后再检查是否出错，这种思想比处处悲观锁逻辑清晰很多



db课里面：修改叶子节点也是这么做的，假定不会出问题吗？



有必要严格界定一下三个状态分别能做什么

Candidate：拉选票时的状态，

Follower：收到心跳包，转为Follwer？收到更高任期的也要成为Follower？Follower的含义究竟是什么

Leader：竞选成功，成为Leader





Q：什么时候有日志分叉？

A：已提交日志是不会有分叉的，但是未提交的有可能，新的Leader有可能有上一任Leader的未提交日志条目，这些日志条目虽然很新，但是最终未必会保留

所以日志落后于本服务器的candidate不会获得投票，但是其实有资格成为Leader

这就是收到更高Term直接转为Follower而不发起选举的原因，因为没办法判断谁更有资格成为Leader。



Q：这又延申出一个问题？副本服务器log可能更超前

A：那就是副本服务器其实可能进度更快，那么新的Leader要如何回退其日志呢？

答案就是超前日志不用管，新的Leader直接按照自己的日志进度来处理。





并发编程的工程抽象





原子化地更新所有内容

start中的投票状态还是要同步vote和append

vote：可能看见更高Term的请求，需要直接打断正在进行的选举

append：可能遇见更高Term的心跳包，也需要直接打断正在进行的选举。

start逻辑拆解：

1成为候选者，原子性地更改任期，更改投票更改角色

2拉选票

3发现获得多数选票：准备成为Leader，但是此时任期可能已经被修改，角色也被修改，所以要检查一下



Q：我什么时候接收投票请求？

A：

此时我可能是Leader：任期低于我，直接拒绝，并且更新其任期，等于我，不好意思，我已经是

此时我可能是Follower：

此时我可能是Candidate：我正在选举呢，都得看你的任期





果然raft是实验最难的一部分诶





错误：重新出现了Leader，但是立刻自己转为候选者，这是因为心跳没有自己压制吗？



选举超时设置为800ms，现在反而选不出来了  





Q：为什么不使用goroutine发送心跳包，选举任期大大延后？



为什么主要分区没有快速发现



那么请问



选举要一定要快速完成哦





我肯定是造成了goroutine的泄露了，就是处理心跳的协程





新的Leader总是自己



看来心跳这个阻塞通道确实有问题呀，因为什么呢，心跳发送后，timer缓存了定时事件





改进：Leader失去Leader地位后，要有种机制完全取消心跳协程的呀。使用Context？现在还没有爆雷







还是出现了多个Leader的情况呀





通过观察发现：reqvotes都是3，4秒钟





之前想到了：对于隔离的Leader，如何让他认识到自己不应该是Leader了？这可能是自己的之前多个Leader的原因





这里一轮心跳包经常3，4s，这怎么行呢？一个失联的服务器同步发送心跳包一直不回应的话，可能导致我一直不给其他副本发心跳包，触发他们的选举

之前就有这种情况吧，所以现在改为异步发送，那么任期递增少了许多





总是出现问题：candidate已经获取了多数选票，就差一步成为Leader，但是这个时候被测试用例检查发现没有Leader



还是没找到那个什么呢，没有找到那个什么呢







3B

日志是从0个开始的

日志索引从0开始，这样prevLog可以携带0

applyCh上

实现选举限制

如果我就是要从0开始呢？那么prev什么的就是-1之类的需要额外判断，index=0，任期为0，那这样

哪里需要使用到这个两个last信息？



拉票的时候要带上

被拉票的时候也要看看



requestVote被拉票的时候如何判断我的最新呢

如果我是candidate，那么日志肯定不变了

如果我是Follower，说不定我还接收着Leader的日志，时刻改变呢

如果我是Leader，我可能真是时刻在改变日志呢，所以这个最后日志的准确性重要吗？







实现选举限制？不就是不允许日志落后的candidate成为Leader，这我3A就完成了呀



Q：raft集群中哪些节点有资格被调用start command？如果只有Leader能接收指令，那么Leader单机是否就是整个集群的瓶颈？

A：确实是这样的，与单机相比，TPS都是下降的

与强一致系统相比，zookeeper，etcd相比是正常水平

raft做的是CP：一致性和分区容忍性。吞吐量不如AP：可用性，分区容忍性。Redis主从节点和kafka顺序日志等





Q：这里是否存在并发问题，比如一个客户端一直追加日志，如何保证顺序提交？

A：当然是按照日志中的顺序



Q：日志的下标是在当前数组中的还是逻辑下标呢？

A：





Q：到这有必要了解一下Golang的序列化方法了

A：怎么完成这个序列化呢



Q：再次理解这个线程开启后上下文的是如何保存的。

A：如果是线程开始，是共用父线程的地址空间页表项的，所以golang整个都是这样子

但是二者不会共用程序栈。数据段，堆段这些确实共用的，但是用户栈这个其实也是属于地址空间的呀，这个怎么分隔开控制呢？

还有这个协程是如何保持堆栈变量不变的呢？





Q：golang中的一个goroutine可以在不同线程之间来回腾挪，那么goroutine执行的上下文是如何保存和恢复的？感觉不是很直观

A：原来都与编译器的逃逸分析有联系





现在的问题就是连续提交多个指令怎么办？





commitIndex,0,1,1

如果有日志空洞怎么处理呢？

或者说怎么样性能最好

是否需要有一种重试机制？



raft不要求任何重试，连续的两个log如果有提交空洞。其实中间可以被自动补齐，因为append会检查日志的下标的。

但是commit游标肯定只检查最近的一个，不会越过空洞检查

不一定能立即向副本发送追加日志，各个副本服务器进度可能不一样，那么还是以一个睡眠循环来发送吗？应该的吧哈哈。总不能不间断地发吧。确实如此

确实没有必要一个个发，立即发送给所有人也没有什么大问题好像。



Q：发现日志冲突，应该立即处理还是延后处理

A：立即处理比较好，因为commitIndex的推进是有速度要求的





Leader相关东西太多了，协程同步是大问题，一旦失去Leader地位，很多都应该同步停止不是吗？心跳，追加日志请求，只有接收方做好防御就行了吗？



Q：获取prevLog是否应该加锁？

A：如果全程加锁，有较大概率造成死锁。

如果只是读取加锁，那么不会死锁

如果不加锁，有可能我在这里发送什么append请求，那边已经Leader任期结束，在接受别人的日志同步了呢。



不要害怕加锁，为了避免死锁，也可以看看代码块中又没依赖其他需要加锁的流程，如果没有，应该不会造成死锁



确实是我想错了，这里不存在什么日志空洞，log得是连续的



确实Leader一旦失势，应该立即全部取消



可取消Chan，一旦关闭，所有监听即刻全部取消，原来是这样呢。



立即取消所有需要回收的协程确实还是有问题，难以实现，根本性如何保证呢

但是根本问题：就算降级为Follower，也无法阻止原Leader继续发送append请求。



raft的正确性又不理解了，防御性编程



Q：日志index+term的唯一性如何保证呢，一个任期内的Leader有可能会以一样的index发送不一样的日志吗？

A：如果本轮追加所有失败的话，下一轮其实还会继续追加呀，追加时候会填补的，所以应该不会发生这种情况，所以index和term就保证了唯一性



Q：为什么要重复试探副本的进度？由副本直接告诉我不就好了吗？

A：新的Leader的日志是最新的哦。

日志的比较先看任期哦。Leader的传承，



Q：是我蠢了，副本服务器不但要复制，也是要apply的

A：





有bug：commit被当成心跳包了

有bug：复制起点从0开始？副本服务器判断冲突有问题



处理日志分叉的情况还是比较麻烦。



家长会，没有得到过爱

不是指令重排序的问题，而是协程乱序执行的问题





把副本服务器的提交检查放在心跳包中



使用time可以查看cpu时间，用户态和内核态分别运行多长时间诶。



蒙蔽了，不知道怎么回事一下收缩到20多s。算了，后面来分析





Q：为了防止分叉日志被提交，需要等到完全log完全追上Leader才能开始提交

A：是这样的，所以这个时候LeaderCommit从0开始







3C

持久化

可能更改持久化状态都要求插入persist调用，是以追加形式调用性能比较好吧。

需要持久化的：

curTerm，当前任期，因为一旦崩溃重新开始确实得有个起点

votedFor，给谁投票了，这个确实，不然可能一个任期多个Leader

log，日志，这个确实是最明显的需要持久化的



前8个字节存储日志内容。后面的就存储日志

回溯部分的优化

append

XTerm：任期内第一条日志条目的索引是否存在





Q：Persister的抽象？

A：

mu互斥锁

state，当前状态

snapshot，快照

封装行为：

拷贝一个持久化对象

readstate，读取状态

statesize，获取大小

save，将状态和快照全部替换

readsnapshot，读取快照

snapshotsize，读取快照大小





需要持久化的就是什么呢



Q：现在的问题是重新加入的故障服务器如果没有新的指令提交就一直没办法完成同步诶。

A：之前我的假设一直是在不断append中完成的同步



Q：奇怪，为什么开头连续两个命令index一样呢



Q：每次都要全部持久化的话是不是性能消耗比较大呀？

A：不知道呢



Q：为什么初始化的时候没有必要持久化？

A：所以这



Q：总觉得心跳程序需要有更大的责任。

A：不是很清楚诶。原来如此吗？

提交指令的追加日志同步

和恢复故障的副本日志同步需要统一处理吗？

感觉是需要统一处理的，因为这个matchIndex的递减就是不能两个同时进行的，因为会出乱子。

每个副本服务器对应一个真正的goroutine，需要Leader主动询问。



这个backup似乎有bug，这个是真的没用啊，真的呀，提交出现了不一样真的是这样的



Q：线程私有变量的内存模型，是cpu机制

A：缓存提升了速度但是不保证线程间的读写的可见性。写完这个我真的去参考一下etcd的开源实现吧



Q：我希望处理的是一旦超时，协程取消啊









0serverindex=2=380，一直不提交

1server也断联，

3server当选Leader，一定放弃，此时，index=2=260





Q：再次重申，一个新Leader当选，只有可能是别人和它日志分叉，不会有自己的日志需要被覆盖的情况

A；因为能当选Leader就是因为它的日志足够新，检查prevLog保证了日志是一致的，连贯的



Q：无语，我之前怎么恢复commitIndex的来着？

A：怎么什么都想不起来啊，卧槽

A：之前是巧妙地通过强制等待一次追加日志来强行校准commitIndex的，现在追加日志完全异步执行，就需要选举完毕立刻校准commitIndex了





Q：要不要把判断commitIndex是否正确的任务移动到副本服务器？

A：真的很难判断这个副本有无日志分叉，必须等到他追上来才



Q：然而为什么现在耗时43s呢？性能是怎么提升的？我真的无语了

A：耗时直接减少30%，我真的无语了有时候。



日志复制，一共148s全部通过



matchIndex是什么呢



3D

日志压缩，状态安装请求，为什么别人都那么有爆发力



现在必须重放完整的raft日志才能恢复状态，然而，对于一个长期运行的服务器来说

Snapshot，服务可以通过该函数传入其状态的序列化快照

index：raft需要丢弃该索引之前的全部日志条目，

InstallSnapshot



怎么说呢，只需要加锁保护log不被此时不被读写就好了

但是呢服务会在每个snapshot上调用。快照是什么意思。



实现installSnapshot



代码结构真的是太乱了，确实是之前读写日志一直没加锁，现在需要锁粗化了



Q：首先一切都是为了保护日志的竞争读写要加锁，导致我必须在任何访问日志信息的地方加锁，所以需要在发送心跳包时候不需要访问日志，所以不用加锁，但是不知道

为什么AppendEntries需要加锁的时候卡住了，然后现在出现了apply不连续的问题，甚至日志回滚逻辑好像也不对了





Q：虽然现在新的Leader能确定commitIndex，但是还是不知道哪些已经应用了。但是这个问题为什么会出现，为什么上一个版本没有出现？

A：上一个版本新的CommitIndex也是什么呢，上一个也有lastApplied记录诶。但是lastApplied需要持久化吗？不太清楚。

之前没这个问题是因为Leader的apply和副本一样的处理逻辑，都有lastApplied





感觉本质上还是对面的appendEntries卡住了的原因



不知道哪里加了锁，现在整个系统全堵了



妈的，现在怎么这么多问题，由于改了下架构，从只剩到一个测试用例都过不去





Leader选举又出问题了？





Q：Append是为什么总是死锁呀

A：



观察：发送两次心跳包，只收到一次心跳回复



原因似乎是因为什么呢，选举超时检查比较长，然后呢，还有就是所谓的什么呢，



好吧，虽然是心跳通道阻塞获取，但是其实呢，不是超时才调用



Q：好像这种多领导的bug确实没有处理过

A：一个副本被隔离久，进入候选者时是上一任Leader的任期，这个时候加入了半数的以前也被隔离的服务器，这个时候他们会允许投票

这个bug要怎么处理？

但是新的集群一定包含旧任期内的Follower，它要如何告诉新的候选者当前任期已经有过Leader？也就是0，6这两个副本投敌了？

成为Follower的方式？要么选举时候

不管怎么样，收到append请求时直接转为对应的Follower，voteFor也给他行不行？行的吧，因为他已经是Leader了，这样我的投票其实是不会计入的

不对啊，对方能成为Leader，那就说明就是获得了这个任期的多数选票，为什么这些人会多次投给一个任期的

真的傻逼啊，modify函数我怎么参数都传错了





有什么不一样

base 的要改一下投票角色转换，3D版也要改一下共3个



总而言之两个大bug：一个是选举时候得票的取消rpc协程不再监听的bug，一个是心跳通道阻塞持有锁的bug，应该不是不能完成什么呢





Q：这下bug出来了：0号一直不知道自己不是Leader了，但是2号为什么要接收低任期的append请求呀？

我一直以为，matchIndex=0了就不可能冲突了

可能的原因就是在请求回来前，Term发生了改变，确实是有这样的竞态条件

其实是是否日志是否冲突在这个竞态条件下具有二义性



Q：这个append试探commitIndex的过程中term改变的情况如此普遍吗？既然已经什么呢



Q：所以，到底在哪里发生了协程大爆炸？

A：我真的无语了



Q：今天犯了很蠢的错误，因为同步发送，我以为异步放入协程

A：cancel很蠢



还真是，我现在是每一轮心跳就创建一堆协程，我为什么不每个副本创建一个协程进行心跳同步呢？我确实是个傻逼，但是这确定是我的问题吗？为什么之前就



为什么之前就没这个问题？



Q：但是这个协程就是有的需要阻塞，本来就会产生巨量的阻塞协程吧

A：为什么现在什么都不行呢，我真的很无语



Q：这么看来我要抛弃大量阻塞协程拖慢整个系统的想法

A：真的是选举实现有问题



循环变量捕获问题，居然一直没发现！



Q：凡是当过Leader的，连选举超时都给阻塞住了，因为分区而失去Leader地位的服务器协程怎么收尾需要考虑清楚！

A：死锁



Q：为什么新的Leader总是立刻放弃领导权？

A：原来是心跳循环退出条件写错了，导致一直在发送心跳呢。



Q：总有一个不投票的家伙

A：凡是当过Leader都不再投票？居然是心跳部分忘记释放锁了





Q：最大的问题是日志读取，这个也应该要加锁的吧

A：如果我在修改的过程中有在读的，会发生并发问题的吧，诶，我还真的都加了锁的



Q：日志修改持久化每次修改都要插入

A：在Leader接收指令的时候忘记了



3D

最重要的是将日志回滚与日志截断相容

需要修改的有两个地方，一个是Leader试探性回溯

一个是同步追加副本日志的协程



Q：选举完我不能立刻提供服务的吧，应该先回溯完？但是现在我是成为Leader立刻就声明我能提供服务了





Q：为什么有个副本的lastApplied丢失了呀，真的是，确实对于一个恢复的什么呢，如何确定自己上一个应用进度在哪？

A：





是啊，apply这块的幂等如何保证呢，同步进行吗，上层服务一时堵塞，那么就可能多开协程去提交，这就是导致多次提交了





Q：观察：测试用例没有特别处理，但是Leader的apply会阻塞



Q：为什么会导致append超时，完全是可靠网络吧，里面没有什么同步阻塞很久的吧

A：apply通道同步阻塞->锁不释放->心跳停止-》新的选举->AppendEntries阻塞

通道不保证公平性



不对啊，我的snap不是保证至少有一条日志记录吗？



Q：拷贝切片需要指定长度

A：这里原来是上层服务准备调用snapshot的时候停止对applyCh的消费了，所以那边直接阻塞。



Q：阻塞通道不保证公平性，为了避免阻塞我使用了协程，但是协程调度顺序无法预知，因此日志条目放入applyCh的顺序可能会有问题

A：之后想想怎么解决



Q：现在还是有同步的问题

A：



Q：没错，任何服务器都必须能够直接恢复状态机的信息，包括快照和未应用日志



Q：切片越界的bug是怎么来的？

A：



现在有3个测试用例过不去，3D的最后三个。而且依旧有nil snapshot的错误，这是为什么，被直接安装快照恢复状态的服务器也要保有快照呢





观察到的现象很是奇怪呀，真的，不太理解为什么，多次install，然后超时？有什么意义吗？对方什么情况呢？多次安装是幂等的吗？

多次安装不幂等

是啊，接口的幂等性如何保证呢，真的不理解  



现在问题是logOffset怎么被改了

从崩溃中恢复：要记住日志第一个条目就是偏移量



Q：lastApplied的故障恢复重要吗？

A：因为可以从快照+日志直接恢复对应状态机的状态，也就是说假设状态机状态为snapshot+lastApplied是第一条是没有什么问题的。



快照日志是59，为什么宕机恢复后成为49了？





通过了测试，但是问题还是没有解决



可是还是有bug的呀



while [ $(tail -n 1 log | grep -c "ok") -gt 0 ]; do go test -run 3C>log;done

跑不出bug就给我一直跑



这么看来乱序没有关系，反正上层服务一直期待着某一个index的命令，现在问题是srv命名有回退，append1号服务器的[9-11]日志，为什么index=9还是自己以前的

首先呢srv2的lastApplied是0就不太对了，恢复之后

其次呢，

在index9日志被134复制之后，1号还是给2号没有完全日志的投了票

1号为什么给没有完全日志的2号投票，index9term53，但是我1号是index9term59，我不应该





好好好，我一号明明是59号任期获得的所谓index9日志，但是确实47？











3C最后的bug是什么呢

Q：新的Leader可能有很久的任期没有提交的日志，这个时候如果追加给别人，那么别人的日志也是很旧的。

这个时候可能中间穿插过一个暂时的Leader，而这个Leader的接收一条日志后写入宕机，但是因为任期很新，他就能够直接当选Leader吗？因为他的任期新？

A：不对，最后的Leader其实已经追加成功了，已经开始apply了，那么他应该可以向上层服务声明已经达成共识了，即便此时有一个有更新日志的Leader也不应该覆盖掉

原来是因为我自之前一直没注意到这个



Figure8的反直觉：即便旧日志已经复制到多数派，依然不能commit



raft必须保证，只有当前任期的日志，才能用多数匹配来提交。

看来还没结束。



禁止直接commit旧term的日志，因为这可能出现幽灵日志。现在我发现确实是这样子



这是因为旧Term的日志即便被多数派apply，还是有可能被稍微新一点的log覆盖掉。原来是这样





Leader如果append指令后可能来不及apply，但是恢复的时候却默认lastApplied进度是最开始的，这里确实有问题





不对：我并没有等待所谓的新的命令提交了再考虑真正移动commitIndex，我还是提交了，因为移动这个是定时任务触发的



任期内的Leader只允许提交自己任期内的日志！

本质上是因为如果当前Leader如果不能给日志留下自己的印记的话提交还是会被覆盖的



为什么解决了这个bug后3D运行时间只需要209s了





好的，3C过去了，现在3B又过不去了，真的牛皮





提交当前任期判断的问题：极端情况，多数服务器崩溃，match可能真就不满多数，这个原因，不过这个情况应该还是挺少的吧。但是呢match总不能真的是那种什么呢其实也是可以的诶嘿嘿。





Q：当选Leader后有必要立刻校准matchIndex吗？

A：校准失联的matchIndex=0的话还是有问题，因为可能再副本的append逻辑中造成切片访问异常。作为Leader进度肯定是要大于等于副本的，所以logOffset也一定大于等于

A：直接假设matchIndex=现在的有问题吗？没有问题！因为发送提交心跳包的条件是matchIndex>=commitIndex，如果commitIndex因为当前Leader的服务而推进，但是实际matchIndex比较落后

新联入集群的话就会收到提交请求，就有问题，所以还是选举开启同步之前完成校准比较有必要



现在3D是311s了，时间又多了卧槽，可是我根本没有做什么影响性能的事情吧
```

Golang相关

```
Golang的一些细节



Q：如果通过Map获取的对象可能是引用也可能是拷贝，如果是struct就是拷贝，如果是引用对象就是引用副本

A：





Q：golang中指针接收者的方法可以作为函数对象传递，但是这样合理吗，方法是绑定对象的吧

A：一开始的违和感在哪？是方法不知道绑定的对象地址在哪。

所以这个方法值本质上是一个闭包，那么我明白了



Q：porcupine是一个用于测试验证线性一致性的Go库，Annotation是用于标记和追踪操作的关键组件

A：





Q：看来golang中goroutine是一种超出引用对象之外的垃圾回收对象，它的垃圾回收需要特别的控制

A：是的，比如time.NewTimer的goroutine



Q：golang的切片底层重新分配数组，引用可能失效？这不太好吧，因此map都需要重新赋值、





Q：golang中的一个goroutine可以在不同线程之间来回腾挪，那么goroutine执行的上下文是如何保存和恢复的？感觉不是很直观

A：

goroutine的上下文不是cpu寄存器

而是

PC程序计数器，SP栈指针，DX专用寄存器，g结构体指针

从编译开始，执行第一个协程的时候，是如何切换的，

第一个协程开始，然后go开启了一个协程，1这个协程可以独立执行吗？协程也有栈的

协程并行执行：协程应该也是有栈的，这个协程然后被另一个M执行着，这个协程应该有着自己的栈，但是呢，两个线程不是应该用不同的栈吗？但是现在看来其实是需要共享的。

另一个线程的栈区被分配了新的物理页并且被拷贝了吗？

协程之间是有依赖的吧

矛盾在哪？

一个协程还没执行完毕，栈区随时更改，要求并行执行的协程能看见

一个新的协程的开始，在另外一个线程上执行，可以更改部分栈区数据，要求原来协程也能看见，但是又是在两个不同的线程上，如何满足？





栈永远私有，堆永远共享，是这样的吗？

原来是编译器逃逸分析的利用，发现栈上逃逸的变量都分配到堆上，这一切都交给编译器决定。原来如此





golang协程捕获变量的逃逸分析：一旦被闭包引用，变量可能逃逸，被分配到堆上。如果打印指针，地址被传递，那么编译器也无法保证这个地址是否存活更久，因此逃逸到栈上

真是很美妙呀



Q：golang的切片不完全是按引用传递，append的结果不会反应到原切片上面。


Q：循环变量的捕获问题
A：
记得循环中开启携程需要捕获非归纳变量
```
