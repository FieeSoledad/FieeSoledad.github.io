---



title: cmu-10714-hw3分析与实现

description: 实现cmu-10714-hw3过程中的思考与踩坑。

published: 2026-03-02

category: MLSys

tags: [MLSys, Cpp]

---

```
构建一个简单的底层库

性能上必须具有竞争力，只比高度优化过的慢2-3倍，朴素实现一般会比高度优化的实现慢上百倍





熟悉一下NDArray类

一个扁平的一维数组如何映射为一个多维接口



大量操作，比如广播，转置，切片只需要调整数组的高层结构strides即可完成





设计哲学就是所有处理数组结构的逻辑都用python进行实现



只有真正的底层代码，对向量进行原始数组操作时，才使用cpp实现





5个重点字段

device

handle

shape

strides，一个元祖，表示数组在每个维度上的步长

offset，表示数组在底层device.Array从内存中哪里开始





参考实现stub，含义

设备后端的灵活性，



NDArray.make，根据给定的参数创建一个新的数组

如果没有指定handle，则分配新的内存







part1

python层数组操作

reshape

permute

broadcast_to

getitem等函数

已经有了一个参考实现，完全基于numpy的



Q：

reshape的逻辑放在make中呢还是reshape中呢

直接在reshape中修改吧



假设[2,3,4]

形状[4,2,3]

strides，每个维度上的步长



[H,W,C]这样子

[2,4,3]

[3,4,2]

[C,W,H]，前8个是通道1的信息是这样吗？不是吧，语义没有对齐



[2,3,4]:strides[0] = 3*4 = 12，strides[1] = 4，strides[2] = 1

但是语义正确吗？

reshape，是什么含义呢



会不会是因为permute语义与reshape不同呢，应该是的，



那么问题是permute如何实现



如何判断一个数组是否紧凑



row-major紧凑，逻辑相邻的在内存中是否相邻

行是否跳着走



reshape是要改变shape还是stride呢

这个不是很清楚诶



理解shape和stride的含义



访问元素是shape和stride共同决定的



transpose只是permute在2维下的特殊情况



某些个维度互换需要strides和shape的维度交换一样一起重排



Q：现在看来这个切片也不是那么复杂

A：只需要复杂调整一下shape和每个维度的步长strides就能完美支持permute，strides等内容。







part2

在cpu后端中实现

Compact

EwiseSetitem

ScalarSetitem







compact：可变数量的循环

setitem类似于compact的反向操作，这个是什么意思呢，陈天奇



如何真正对数组进行赋值

.compact会将数组拷贝到新的内存吧，因为是紧凑一下了

在这里行不通，为什么行不通

setitem是修改原数组已有内存？



compact是将view变为独立的副本

setitem是通过view修改原始数据





这个可恶的AlignedArray为什么这么设计



Q；为什么setitem非得是紧凑数组

A：用个非紧凑的也行吧，不过就是寻址的时候需要加上一层三维转换罢了





part3

cpu后端，逐元素与标量运算



记住power有遇见异常





part4

cpu后端的规约操作





Q：若切片为-1，如何调整shape和strides达成这个目标



Q：对于紧凑compact的数组，沿指定轴进行求和或者取最大值在实现上也可能比较麻烦

A

会吗？

不是不断乘上strides进行聚合就可以了吗

原因

先将规约轴permute到最后一个维度

然后对数组调用compact，这是reduce_view_out所做的事情







part5是什么呢

实现矩阵乘法

分块乘法

alignedDot：向量点乘





明天就是cuda版本的ndarray后端了









Q：为什么现在不将参数封装为一个一维数组了

A：不太清楚诶，真的是不清楚





Q：这里指针怎么转为地址呢？

A：应该是直接取地址就行了吧？



Q：不太清楚为什么out要累加





part6

cuda后端

compact紧凑

setitem



cuda实现与cpp实现的关键差异

基础函数与cuda kernel的区分

每个kernel的实现都需要两个函数：

base function，负责参数准备，kernel启动等

cuda kernel，真正执行计算的gpu函数



cudavec结构体

向cuda kernel传递shape和strides等参数





cuda kernel不能直接操作stl容器

解决方案是：

1将vector转换成普通的cuda数组

2使用cudaMalloc分配内存

3以整数指针形式传递参数

4在kernel调用结束后释放内存



太繁琐：定义一个具有最大维度容量的结构体，将实际的shape和strides存在在前若干元素中





cpp和cuda实现的核心差异：

cpp实现中遍历非紧凑数组的元素，通常对索引计算进行一定优化，复用中间结果

但是cuda无法这么做，每个线程只负责一个元素



kernel是在gpu上运行，cpu调用

device是只在gpu上运行，gpu上调用的函数



首先实现compact

实现ewisesetitem

在实现scalarsetitem，你，



grid：一次kernel启动的全部线程集合

block：线程块，grid中的分组单位，

thread真正执行的最小单位



gridDim：各个维度上有多少个block

blockDim：各个维度上有多少个线程



blockIdx

threadIdx是什么东西





Q：cudaArray是什么结构

A：是调用cudaMalloc申请的一块内存



Q：cudaDims是什么结构

A：

gridDim还有blockDim，描述各个方向的维度





Q：cudaOneDim是如何创建的cudaDims的

A：base_thread_num是256



Q：Compact和ewiseset有什么区别吗？

A：

compact是将origin数组转为行优先紧凑的方式，所以out的索引不用计算

ewiseset的out索引也不用计算，一样的索引方式。





cudaOneDim怎么说呢，

哦纳囊笑死了我都，真的呢

现在问题是之前的那个为什么是行紧凑了。

对于一个非紧凑，的地方



Q：用紧凑的数组非紧凑的数组赋值

A：这里相对于cpp编程的核心差异就是，cpp一个线程处理所有

而现在是一个线程处理一个元素，必须知道当前线程处理元素的位置，然后去找从原数组那些位置得到现在这个元素



对一个索引，在当前shape和strides下得知道是行优先下偏移了多少个位置，然后直接索引行优先数组



现在问题是cuda的power如何计算，有cpp库可以调用吗？





Q：单元素的实现比较好求

A：确实都要认为他们是一维度数组，然后找到当前strides表示下非紧凑数组的坐标就行了





Q：还是cuda版本的规约操作

A：



简单实现版本：如果有100*20的数组，axis=None

朴素实现是直接开启100个线程分别规约



工业级的高性能版本是采用分层机制：

先在较小的跨度内进行部分规约

再对中间结果进行规约

以此类推





最后一步，实现cuda的矩阵乘



cuda高性能计算的几个tricks：

1量化，降低精度，减少内存移动

2重计算

3算子融合

4内存合并访问

5堆叠，似乎和上一个没什么区别



内存堆叠


合并写技巧
```


```
通用知识点总结



第11讲

通用加速技术

向量化

数据布局与stride，row-major

这个实现了零拷贝切片，但是是非连续访问，向量化困难

并行化



内存层次结构意识





cuda的硬规则：

threadIdx.x是连续线程维度

目标让一个Wrap中的线程访问连续的内存地址

所以必须要求threadIdx.x对应于col变化
```
