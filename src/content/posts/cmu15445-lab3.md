---

title: cmu15445-lab3分析与实现

description: 实现cmu15445-lab2实现过程中的思考与踩坑。

published: 2025-10-20

category: 数据库

tags: [数据库, Cpp]

---



```
此前，已经实现了缓冲池管理器和并发版本的b+树索引



实现能够执行查询的组件



创建用于执行SQL查询的运算符执行器



编译终端：make -j4 shell

./bin/bustub-shell



任务1：访问方法执行器

任务2：聚合和连接执行器

任务3：哈希连接执行器与优化

任务4：外部归并排序+限制执行器





使用的火山模型

创建操作符执行器

操作符简介

投影projection：对输入进行各种计算

列值表达式

常量表达式

算数表达式



过滤操作符

一个孩子节点一个谓语predicate



values节点



什么SQLLogicTest生成的sql

executor_factory



task1

不支持Drop数据库，需要重启shell

seqscan

insert：还需要更新索引

update：先删除目标元组，再插入新元组

delete：记得更新索引

indexscan：使用b+树索引执行点查询和有序扫描

将seqscan优化为indexscan

实现此优化器规则：

启用谓词下推到seqscan

使用索引

TupleMeta是什么呢

TableHeap是什么呢



整体代码设计：

AbstractExecutor：执行器抽象类，有执行器上下文

ExecutorContext：执行器上下文，有事务管理器，缓冲池管理器，目录类，当前事务，锁管理器

Catalog，目录类，成员缓冲管理器，锁管理器，日志管理器，表id和表信息映射，表名和表id映射，下一个表id，

索引id和索引映射，索引名和索引映射，下一个索引id

一般情况下，不需要根据索引名查找索引，而是根据表名查找索引名

封装行为

创建表，表名也是索引，好像是创建一个空表的吧

获取表

创建索引，

获取索引，

获取表索引，

问题：已有的表名难道不读取了吗？序列化系统状态似乎还没有实现





AbstractPlanNode：输出模式，孩子计划节点。

SeqScanPlanNode：表标识符，表名，过滤谓词，封装行为：传递孩子节点进行克隆

AbstractExpression：抽象表达式，抽象表达式孩子节点，返回类型Column



SeqScanExecutor：顺序扫描执行器，成员顺序扫描计划节点



Tuple，元组，成员RID和data_数组，有构造函数依赖Values和Schema

构造函数逻辑：

首先计算整个元组数据所需要的长度

遍历Value，先填入每列的偏移量，再填入每列的数据到Tuple的data_指针处

序列化的时候有拷贝变长数组的长度



Schema，模式，成员长度，Column数组，是否所有行都内联，未内联的Column标号数组



Value，值，成员val联合，可能有多种值，typeid值类型，size值大小(长度)，manage是否有数据。

封装获取当前类型的长度的方法：获取当前typeid的Type实例，传入当前Value对象

为什么绕了一圈？保证扩展性，新增类型不必修改Value



Column，列，列名，列类型，长度，在元组内的偏移量



Type和各种的BooleanType的关系是什么



TupleMeta，元组的元数据，时间戳，删除标识符

TableHeap，成员缓冲池管理器，第一页id，最后一页id，互斥锁

封装行为：主要是一些获取元组，修改元组和元组的元信息的方法

插入元组，调用page的插入方法

TablePage，成员，page_start数组，下一页页号，元组数量，删除的元组数量，元组信息（int，int，TupleMeta）

封装行为：设置成员变量，获取元组

插入元组：使用一种紧密的内存布局，尾部零长度数组

TableIterator，迭代器，成员，table_heap的引用，记录id，停止的记录id

迭代器的自增，我有保证说前面的页面页号一定小于后面的页号吗？





TableInfo，表信息，模式，表名，TableHeap指针，表的OID

里面的索引有4种，b+树索引，哈希索引，stl有序索引，stl无序索引

IndexInfo，索引信息，成员模式，索引名，索引指针，索引标识符，表名，大小，是否主键，索引类型

索引，索引元数据就有维护索引的列索引集合





Q：在目录类种创建的新表只是有一个空白页吧

A：如何将这个TableHeap当成一个b+树进行插入呢？不能的吧。

bplusTree将指针AsHeaderPage然后Init？

识别一颗二叉树就是使用header_page_id即可



Q：table_page和b+树的叶子节点不一样的吗，存储就是存储数据元组的，而非主键索引

A：确实不一样



Q：table_heap插入新记录时，和聚簇索引如何同步呢？

A：使用主键索引就是了呗



Q：现在要思考的就是什么呢，执行器需要保存什么状态，是自己保存还是依赖其他成员

A：



Q：为什么seq_scan_executor不需要孩子执行器，而insert_executor需要？

A：因为seq_scan_executor就是操作符，就是数据源，因此是叶子节点。

而insert_executor是需要知道从哪里获取数据的，需要孩子节点提供需要插入的数据



Q：知道为什么抽象执行器类是需要多个孩子节点了吧？

A：就是因为什么呢，因为有的操作符需要操作多个表的原因吧



Q：这么看插入是页面顺序插入，只有索引是需要b+树插入的是吧，然后索引扫描了一页但是还是可能是随机访问呢？

A：这么看应该是的



Q；元组对象如何设置值呢。

A：初始化一个value数组和一个什么数组呢，调用其构造函数



Q：当你不知道某个机制如何实现时，可以考虑系统中有没有哪里有这个机制

A：比如这个插入索引在创建索引的时候就有了



Q：执行器，执行计划，输出模式保存在哪里，为什么这么设计？

A：输出模式保存在执行计划中，但是执行器有一个接口获取输出模式



Q：为什么update的执行器会多出一个表信息字段



Q：智能指针赋值给裸指针，有没可能不安全

A：智能指针的引用计数会改变吗



Q：能理顺一波更新逻辑吗？如果元组中有变长属性，怎么更新？

A：似乎是大小不一样会直接报错



Q：update的更新索引逻辑是什么呢

A：有可能修改的字段就是索引的字段吧，那么先



Q：索引扫描执行器，怎么扫，扫出来什么呢？

A：怎么扫应该有一个谓词来定义过滤，由执行计划提供

A：事实上一个索引的主键可能是由多个键组成的吧，

A：索引计划节点的输入和输出是什么呢？

假设现在只支持一个索引的情况，按照迭代器扫，是先扫描出来再进行什么呢，



Q：合并过滤扫描什么意思？

A：优化器是如何替换原始计划的。查看第一个优化器，查询优化



Q；为什么这里抽象表达式就是谓词

A：其实应该有很多方法的吧，可能返回值，也可能只是用于做判断



Q：为什么现在我迭代不加const不报错



Q：如何匹配索引？

A: 忘记了，这个只支持单列索引好像？



Q：顺序扫描优化为索引扫描，查看如何从谓词推断出使用的列

A：看一下其他类似的排序优化器的实现，发现这里有好多的表达式

哪些谓词是可以下推的，那就是比较表达式，常量表达式吧

只在3种场景下优化：

1一个等值测试 v1=1

2索引列顺序翻转1=v1时

3同一索引上有多个点查找时 v1=1 or v1=4

使用多个列的情况下依旧使用顺序扫描，不做优化



Q：如何查看执行计划节点的树状结构？

A：好像不能查看



Q：这个先存在的表从哪里读取的？

A：答案是初始化时通过GenerateTestTable生成的数据。



Q：初始化测试表的时候有维护主键索引吗？

A：没有，先吧主键索引视作和一般索引一样的东西



Q：执行器的生命周期是什么样的？

A：我应该保证执行器保留什么状态？看看执行器是如何组装起来的



Q：如果update从孩子节点读入数据需要先初始化，那么这个流水线处理是怎么来的？

A：责任链，要在Init中初始化自己的孩子节点



Q：这个seqscan的执行器中谓词似乎我没写？

A：是的，得将这个谓词作用于tuple

如果是set colB=15445这样的赋值语句，那么谓词应该是算法表达式。

不对，更新的谓词是在update的执行计划节点中。列和目标值交替存储。是这样的吗？



Q：看看这个TableHeap的设计是不是没有检查表是否为空，返回的迭代器报错了

A：是我seq_scan实现的错。



Q：index这个，有了index的schema，schema应该有保留行号索引。

A：indexscan的实现，首先本身有一个谓词，谓词只使用了索引的内容

b+树的话是使用迭代器吗？迭代器的话，就是每个key扫出来过一遍谓词。但是这个太低效，使用索引的目的就是为了logn的复杂度

还是点查询，点查询需要提供key，那就直接使用谓词的常量构造一个key，然后查询。



使用点查询的话，一次也会查询出来很多个，还是在初始化的时候就全部扫出来，然后在next中迭代比较好吗

如果是点查询，是否可以给index定义一个过滤谓词，不需要，因为这是点查询，不能一次查询多个。

最好还是初始化的时候全部查出来比较好，多个值的话也不知道查什么呢



谓词的分类，这里有的谓词可以用于计算判断，应该说，seqscan和indexscan中的谓词都用于计算判断是对的。

但是呢seqscan是先扫描再过滤，indexscan是构造key进行点查询，用不上抽象表达式的evaluate。



先收集所有的constant表达式的value，然后构造tuple，然后构造key，然后查询是吧。



怎么说呢，相当地不优雅这个实现，什么的呢，笑死了





scankey可能很有帮助，这个是做什么的呢，直接从索引中获取结果罢了

aggregatekey在聚合计划中可能也有帮助，在处理多个表达式的场景中

这个聚合有多个groupby，多个aggragate聚合函数也正常的吧

到底允不允许索引树存储重复键呢，奇怪。

应该是不允许吧，只是传入的数组是为了以后扩展允许重复的键



现在的主要目标是理解这个谓词结构，如何判断它是我想要的



RTII内置类型识别是什么原理呢？没有虚函数表，没有Java的方法区





这个树结构如何优雅地检查呢。

今天改写了tuple设置单个字段SetValue。

Value对象重载了比较，这样红黑树排序去重也能支持也能支持。



task2

聚合执行器，不是join

聚合执行计划的输出模式由分组列和聚合列组成

min，max等使用基本用在分组统计的情况下

having会在此聚合计划后面再来一个过滤计划

实现聚合通常使用的是哈希表，假设内存装得下，不需要多阶段分区和缓冲池页面支撑

分组列永不为null，如何保证的，聚合列的null值处理

内部封装的哈希表，key和value封装了什么

aggregatekey是分组键，aggregatevalue是分组的值

aggregatekey需要实现哈希法，使用本系统提供的类似字符串哈希的方法

Q：aggregatekey要存什么我知道，但是value要存什么呢？

A：一般应该存储对应的元组的对象引用的，然后迭代哈希表去聚合每个哈希表的值。

原来是直接存储聚合的结果，迭代的过程同时计算呀

支持五种聚合方法，count*，count，max，min，sum，没有avg吗？avg在迭代的过程中同时计算似乎会导致比较大的误差。

对于空表，countstart返回0，其他返回都是integer_null，为什么count不返回0呢？似乎可以带数字。

聚合哪一列由表达式ColumnValueExpression给出

所以这个哈希表构造参数要构造表达式是为了什么呢。

这个agg_key就没有什么是放在栈上的吗？没有吗？初始化pair的时候拷贝赋值了

这个迭代器就是给unordered_map的迭代器进行了包装而已



这个聚合的表达式可以是ColumnValueExpression也可以是ConstantValueExpression（在countstar）时候



Q：如果我输出分组列和聚合列写反了会怎么样？max(colA),colB

A：不会怎么样，本节点的输出模式会保证分组列在前，分组列在后，只是后面会添加一个投影计划。



这个distinct去重逻辑很粗暴，直接给分组列，但是不给聚合列。



count(col)为什么要那么设计

不给分组，一个null，这个时候不给分组也没关系，空值在也能哈希

给分组，多个null，这下也能统计不同值的个数了

事实上count(col)是统计col中非null的行数，那为什么一开始不赋值为0而是null？

如何优雅实现

这里如果是空表，那么哈希表一个值都不会插入

只能看聚合表达式了

统计空集中的有效值是无意义的。



所以如果是空表，分组列是无意义的，全部为null，聚合列只有数行数是有意义的



Q：schema哪里存储了列名

A：在column中存储



实现嵌套循环连接

区分内连接和外连接

inner join显式内连接，还有隐式内连接

外连接区别在于是否保留null值。

连接有四种类型，内外连接，左右连接

Q：这里的内连接谓词应该是一个等于表达式，能否使用抽象表达式自动计算来实现

A：答案是没有问题有EvaluateJoin这个方法

Q：Tuple的是否提供合并两个tuple的方法？这在连接表的情况很有用

A：没有，所以我可能需要在tuple中实现。

Q：我记得这个是有记录使用左表还是右表的字段的，难道的ColumnValueExpression？

A：是的，好像join计划节点的输出模式没有给出是左表右表的哪一列，因为join是全部保留，提取字段交给后面的投影计划。

Q：连接输出模式的内存布局应该是什么样的？

A：打印一下，查看好了。其实呢不是很影响，我需要维护偏移量吗？输出模式

Q：偏移量是谁维护的？输出模式执行计划能直接排好吗？非内联列的偏移量要构建执行计划还需要遍历一遍元组吗？

A：内联列的偏移量是维护好了。这个问题只能暂时搁置

Q：有点完蛋，这个右执行器好像是需要多次执行的？Init能重复调用吗？

A：这么看来所有扫描执行器确实要实现多次调用的功能。

Q：连接只有等值连接一种情况吗？不，有很多种情况。所以左连接的判断还是有点困难

tuple的内存布局：

内联列-非内联列偏移量-内联列-非内联列长度-非内联列数据



实现嵌套索引连接

确实原本就有嵌套循环连接优化为索引连接的优化器，逻辑是什么呢，准备做到什么呢

优化器逻辑：如果计划的谓词这个是等值比较，然后左右都是ColumnValueExpression表达式，如果右计划是一个seqscan计划

等值表达式的左列来自左表，右列来自右表，则查找右表是否有索引，有索引则生成优化查询计划。

若左列来自右表，右列来自左表，依旧是查找右表对应列是否有索引，若有则生成优化查询计划。



生成的新的查询计划NestedIndexJoin的子计划可能有几个？

1.左表seq_scan_plan，右表index_scan_plan，不行，index_scan是通过谓词直接构建key去查到所有的元组。这里计划太多了。

2.左表seq_scan，右表nested_index_executor，在nested_index_executor中构建多个index_scan_executor去查找还是依赖别的接口呢

构建index_scan_executor，需要自己构建查询计划

依赖scankey接口：需要额外自己去用table_heap读取元组。我觉得这个好。



Q：问题是索引不支持重复键呀，这可怎么办呢。

A：暂时没想到



Q：扫描索引肯定从左表构造Tuple，然后扫描右表，具体如何实现？

A：从左表tuple获取指定列的value，构造指定列的schema（利用Schema的静态方法Copy），然后初始化tuple。

这个CopySchema是否修改列的偏移量？完全不修改。虽然影响可能不大



Q：这个SetFromKey是假设tuple里面只有比较键值



Q：我不想多检查一遍怎么办，如何利用前面的比较逻辑。

A：没办法，短路使用lambda表达式也不能直接返回



Q：似乎db从最顶层获取输出行无视类型一直调用next的

A：有点逆天



Q：logictest测试暴露出什么问题

A：一个就是insert插入0行的情况没返回

一个是update v1 = v3+1;这个谓词结构什么样子

这个delete的测试用例无语了，自己少了两个分号

indexscan对order没有支持，BPlusTreeIndexForTwoIntegerColumn是什么类。

一个空表的扫描有问题。迭代器的问题

聚合中input和result的结果不一致，所以聚合表达式能是什么类型呢？除了列表达式，果然还有很多奇怪的加减表达式。

这个分组聚合，还有group by v1+v2这种情况

合并元组还是需要支持所谓的变长字段的。

两个表，githubid，officehour

day_of_week，has_lecture

明白了：这个不是内联长度的含义不是内联字段长度的含义，而是内联字段+非内联偏移量地址长度的含义

不禁想到，这个长度不允许改变的话该、

很巧妙的设计：如果列是非内联，自己就知道offset是一个新的偏移量而不是数据。

合并后的右表的列offset确实没管变长字段，直接加内联长度。这个能支持插入另一个表吗？

它真的要求我实现左连接

看见这个包装器模式了，挺好玩了，添加记录调用次数的功能。太逆天了，原来是我不小心释放了左执行器的内存

内连接可以连接多个条件吗？不知道呢

如何定义null呢？对啊，我没想过mysql这样的数据库，如何定义一个整数是个null呢？从持久化的数据直接判断是个null？

bustub中使用的是所谓的极小或极大数代表null，但是会损失有效值，在mysql这类数据库中使用的是位图表示null。

10的测试用例有问题，一个是(2, 45)自己的v2，一个结果集少一个，一个多一个，一个是t3插入了两次。

distinct的计划怎么实现的，group by实现的

莫名的空指针问题，莫名的无法创建索引。空指针问题解决了，是init状态不一致的问题

确实要保证整个执行器可以重复使用呀。确实是这样。因为loopjoin等执行器可能需要多次初始化一个执行器。





Q：为什么索引扫描计划会有filter和pred数组两种谓词？

A：pred是一个数组，用于在多个点查询的时候将常量放置于此，indexscan的优化计划是我负责的，应该我来实现

Q：看看这个有order的排序计划是如何优化为索引扫描的

A：支持3种排序方式，默认，升序，降序。默认等于升序。不支持多个字段的排序

这个获取索引迭代器需要指定泛型，感觉好奇怪，使用他就必须知道类型，一个泛型扩散太多了吧

创建索引的时候使用的是泛型方法，使用索引的时候不需要，因为其父类不是泛型，父类提供的方法也不依赖泛型？所以没办法提供返回

迭代器的方法，还是要下转。而且这里已经固定索引是什么类型了。



原来有SQLLogicTests可以用来测试呀



更新不允许变长字段





哈希连接的实现

和聚合执行器一样是一个流水线中断器，需要所有的输入才能得到输出。

实现内连接和左外连接。

多属性的比较也要支持。

分组聚合的时候用到hash，这个也用到哈希。

嵌套循环连接和嵌套索引连接，哈希连接，什么时候使用哈希连接呢。

确实这个clone的意义是什么呢。每个优化器都要递归地检查所有孩子节点，确保优化所有可优化的地方。

我们已经使用了智能指针，没人用的时候会自动回收掉。为什么clonewithchildren可以直接赋值拷贝。

两个表的连接的条件谓词未必涉及两个表的字段



后面还要理解一下执行器是怎么构建起来的。这边只是有个计划而已。

实现这个哈希函数和Java有什么区别？

java中的hash是祖类的方法，所有子类都有，直接调用即可

cpp中的hash函数调用的是模板特化的hash函数



外部归并排序执行器

排序时如果有索引则用索引，其他情况则是使用外部归并排序执行器

limit执行器实现就简单得多

SortPlanNode

如果是有几万个页面的大表排序，是否有爆栈可能性呢？一般程序的栈区有1-2MB，二十多层的递归可能还不到爆栈可能吧。









这个外部归并排序是怎么排的呀，二路归并我想要知道谁大谁小，哪个的指针移动

但是多个的话，Heap<K>



我真蠢，怎么能改变局部区域的指针地址呢。



Q：为什么页面元组要内联大小+4来着？

A：因为元组序列化时还有存储一个元组长度呀。







写完了测试一下性能呢，测试缓冲池大小参数和归并数对性能的影响，多路归并，磁盘io的次数变少，性能提升还是比较明显的。
```

Cpp17相关

```
cpp内容

Q：什么是constexpr

A：cpp11引入的关键字，用于声明编译时常量和在编译时求值的函数，将计算从运行时提前到编译时



Q：内存布局小技巧：零长度尾部数组

A：访问数组元素：当前对象地址+对象大小



Q：dynamic_pointer_cast和dynamic_cast区别是什么

A：pointer_cast作用于智能指针，dynamic_cast作用于裸指针



Q：智能指针如何获取裸指针？

A：使用get方法



Q：dynamic_cast如何保证运行时安全？

A：对象头信息真的有吗？回头看看



Q：unique_ptr指针在系统中什么大用？

A：最多是用来标识这是一个独占资源，禁止转移吧。

unique_ptr可以转为shared，反过来不行



Q：我大概明白了为什么了

A：执行器的析构函数为什么没看见？其实看见了。



Q：智能指针应该怎么用呢？

A：标识一下资源属于谁，在没有引用的时候自动释放资源。



Q：体会到cpp为什么不安全吗？

A：数组越界无法判断，需要手动检查。



Q：cbegin和cend的c是const，不允许通过迭代器修改值



Q：什么时候存在内存泄露？终于让我检测到了。

A：我在nestedloopjoin中申请了自己的内存，是一个new创建的堆指针，拥有自己的资源，难道析构的时候不会调用吗？

析构的时候只删除指针本身，不会主动delete指向的内存

对象能自动析构

为什么其他没有出现内存泄露？因为都是那都是tuple栈上分配，本身tuple没有持有资源。现在是tuple持有资源，然后获取其他资源

时候没有释放已有资源。这是错的，因为那也不叫持有资源，拷贝的时候递归调用自己的拷贝字段，内存是覆盖的，不需要拷贝

我添加了释放已有资源，但是没有拷贝呀，我真的被自己蠢到了。

delete之后指针不会变成nullptr



智能指针如何解决这个问题？

这主要是因为普通指针指向改变后不会调用delete，智能指针指向转为nullptr之后会怎么样？在拷贝赋值重载中释放已有资源

是智能指针的机制。





Q：智能指针支持解引用赋值吗？这样是否会绕开检查？思考智能指针一定安全吗？取*是否会绕开检查？

A：支持的，只是如果赋值是拷贝赋值也没什么，因为本来就是复用内存资源。

Q：智能指针如何更换对象，直接赋值可以吗？

A: 独占指针的语义是资源独占，需要移动，共享指针是增加计数。



Get：

1.内存泄露主要是裸指针指向改变时不释放已有资源，智能指针会在这种情况下自动释放资源。

对象析构的时候也不调用裸指针对象的析构，这种设计哲学是因为指针不表示所有权。



Q：自己定义了析构函数，编译器不会自动生成移动赋值等。rule of five

A：那五个函数得一起定义



Q：const形参不能接收非const，但是变量可以

A：编译器的问题吗？



Q：智能指针的多态如何实现？

A；本质是拷贝构造和赋值重载为了模板方法？而且不会抑制默认生成。



Q：一个模板类中再有一个模板方法。只是这个模板方法的类型是如何特化的？

显示实例化时，模板方法中的模板参数是什么

使用的地方都知道，编译时所有模板参数都是已知的



Q：编译器如何判断一个对象是否可迭代

A：基于begin和end等自由方法或begin等自由函数。



Q：模板特化必须在第一次使用之前定义吗？

A：确实是的，编译器使用即编译时



Q：原来返回的是对象的时候，可以直接返回一个{}吗？



Q：cpp中优先级队列如何编译时约束模板参数的



Q：成员如果没有默认构造，那么类也不会有默认构造了。

A：内部类可以独立于外部类存在，想要访问外部类需要传递引用
```
