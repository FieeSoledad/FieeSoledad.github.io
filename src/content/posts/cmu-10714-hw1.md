---



title: cmu-10714-hw1分析与实现

description: 实现cmu-10714-hw1过程中的思考与踩坑。

published: 2026-01-11

category: MLSys

tags: [MLSys, Cpp]

---

```
Value计算图中的一个值

子类Tensor



Op计算图中的一个算子



TensorOp，Op的一个子类，专门用于返回Tensor的算子





需要实现的

1前向传播

2反向传播

3拓扑排序

4实现反向自动微分  

5softmax

6SGD for two layers





反向传播这样做的原因是我们需要梯度本身也能通过计算图构建出来





什么叫做反向拓扑遍历时所执行的操作，免费获得高阶导数



按照拓扑排序的反向顺序遍历计算图，构建新的伴随节点adjoint node是什么意思



将结果存储在每个tensor的grad字段中















Q：思考啊，他让我训练，现在有了XW等上下文，我的计算图在哪里呢？

A：

看看这个抽象，前向传播在哪

反向传播封装在tensor中，tensor就是计算图节点诶，有记录inputs，grad等内容

反向传播可以直接调用compute_variables，那么前向传播呢？

之前的前向传播：拓扑排序后，一个个计算，现在好像没有提供这个接口



cached_data就是当前张量的值，

realize_cached_data就是递归调用inputs节点计算输出，在当前节点再继续前向传播，算是一个递归调用





现在问题是什么呢

cached_data是ndarray，但是ndarray如何转Tensor？



Q：现在的问题是如何构建一个持久化的计算图

A：

或者是计算图初始化的时机是什么？



思考pytorch是怎么写的？

A：pytorch的数据输入就是tensor，那么答案就是numpy直接转Tensor好了

tensor.add。自动微分框架



我又要看前向传播触发时机在哪呢？



确实，现在他们的关系给我难住了



现在是每次前向传播都会重建计算图，因为Tensor是新的

但是W1，W2这些神经网络中的模型参数一直是同一个Tensor，所以W1这些是作为叶子节点被更新的

计算一次grad，重新compute时，grad会被替换掉，backward一次grad就是新的

然后他这个compute_gradients好像也只是构建了一个反向传播计算图，具体的计算究竟发生在哪一步呢？

什么时候





Q：之所以一直递归，肯定就是连着就的计算图了，确实是这样的

A：W1指向一个加减节点了，有input了，但是我们要求W1一直应该是一个叶子节点



Q：为什么反向传播中可以获取输入的形状？

A：因为前向传播已经完成了，肯定已经计算完毕了



Q；为什么现将消失的维度补回1，在一次性广播回输入形状？

A：不太清楚诶
```
