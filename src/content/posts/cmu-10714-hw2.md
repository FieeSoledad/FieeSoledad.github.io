---

title: cmu-10714-hw2分析与实现

description: 实现cmu-10714-hw2过程中的思考与踩坑。

published: 2026-01-14

category: MLSys

tags: [MLSys, Cpp]

---



```
hw2

这次是实现ndarray





Q1

实现几种常见的初始化方法



Xavier均匀初始化

Xavier正态初始化

Kaiming初始化



什么是初始化



Q2

实现这么多算子吗



Q：Tensor在什么时候可以获得形状，很多涉及到broadcastTo的算子，构建计算图的时候需要形状，怎么才能拿下？

A：



Tensor的存在就是与一个numpy绑定的，整个计算过程形状应该不会改变

所以shape需要调用计算也没有关系，因为Tensor内部有缓存机制，早计算与晚计算区别不大



原来我早就已经开始访问Tensor.shape变量了，reshape节点是知道输入节点的形状的，所以反向传播也是不难



要求实现参数-变量

Q：Parameter和Variable有什么区别

A：

其实都是用来占位的





Parameter这层抽象是为可训练参数提供一个同样的类型，方便优化器发现他们



那么奇怪了，之前的Linear层为什么会被访问呢bias呢，明明要求是加在Parameter层中



Q：这些线性层与原本的计算节点Tensor是什么关系

A：

线性层封装了forward方法，Tensor只在forward中传递，forward中要一直保持为Tensor，通过forward的计算构建的计算图

原来是这样吗？

很有意思



Q：小bug：grad偶尔会变成Tensor





sum之后这个也不苦难呀，为什么会变成单元素呢

python使用type查看节点的运算符类型，还是可以的，真的很不错哦，forward(node,out_grad)

node.inputs()，底层算子给你实现一下吧





反向传播梯度的方差是什么呢



数值稳定性，logSumExp的数值为什么不考虑下溢，因为不是问题，那些项本身相对于最大值已经可以忽略了





Q：简单来说，现在我懂得了如何按照最小步的反向传播然后组合成复杂的函数自动微分

A：

现在这个复杂函数我能用什么技巧直接给出结果呢

矛盾点在于，backward是一次性计算所有节点的梯度表达式

而我现在只是需要一个局部的梯度表达式，没有这个api。





Q：我总是忘记loss要除上样本数，如果不除样本数会怎么样

A：那么相当于没有充分利用样本信息。有很大的噪声



Q；层归一化的实现

A：

每一个mini-batch需要应用层归一化，







Q：这个batchnorm总是running信息出错





Q：Dropout层次

A：还要除上1-p，这是为了期望不变

每个神经元以独立的概率设置为0，怎么做





Q：残差连接为什么是用加的



Q：问题是层归一化中，weight和bias如何初始化

A：正在做



这个展平层其实就是内部封装了一个reshape算子罢了，所以我为什么会认为







Q：开始实现优化器的step函数

A：

确保优化器不对张量的梯度进行原地修改

实现带动量的随机梯度下降





Q：优化器这个参数，params应该是有所有待更新的网络层

A：

现在继续下一步



Q：梯度更新

A：我要更新的是原本对象的cached_data

这个时候



Q：L2正则化的原理

A：





Q：Adam优化器继续

A：

带动量的SGD+每个参数的自适应学习率

问题：不同参数的合适步长不一样

有的参数梯度很大

有的参数梯度很小

用同一个学习率：大梯度参数震荡，小梯度几乎不动

一阶矩：梯度的滑动平均

二阶矩：梯度平方的滑动平均：本质是梯度大小的估计，衡量这个参数更新得有多剧烈

然后自动调整学习率



这个勾八，所有data都是张量，底层只是调用了detach，初始化一个





Q：大坑：所有待训练参数都要使用Parameter去包装。

A：

偏置，线性变换，这个是仿射变换。



Q：实现数据库的基础组件，关于什么呢，无语子了，真的很无语哦





Q：继续实现

A：所谓的



Q：zip能直接作为可迭代对象使用，也能直接返回每个维度的迭代器

A：

dataloader中就是这么用的





Q：为什么要求dataloader返回调用numpy？

A：





Q：想不到一个小小的DataLoader都能这么麻烦

A：

约定什么呢

看来也不是那么困难，就是return的也不一定是什么



Q：最后实现一个残差块

A：

为什么紧跟着的残差块的hidden_dim要慢慢减半







Q：第一个除0错误

A：什么原因，Linear层的初始化不对吗，



Q：一开始实现的残差块归一化维数没有用隐藏维数

A：经过残差块，维数是不变的，因为两个线性层



Q：resnet.forward还有问题

A；

是什么问题



batchnorm是不对的，有问题的



Q：居然是Dropout的错误

A；真的很厉害



数据形状不一致

还有这个float64究竟是哪里来的



Q：我为什么会自动忽略形状问题

A：还有数据类型问题

清空梯度



最后一个数据问题了，



不可逆得变成了float64，这个在哪里检查，哎呀，说实话，已经多加几个测试用例





Q：哪里来的数据类型隐式转换

A：

numpy为什么默认是浮点数呢？

bool值/浮点数都是float64，就是这一步了

numpy默认的浮点数推断是float64



Q：所有的bug

A：

有计算err和loss时候类型对不齐，opt没有reset清空梯度，维数问题等

最可怕的还是dropout，错误很多
```
