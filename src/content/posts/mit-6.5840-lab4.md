---

title: mit-6.5840-lab4分析与实现

description: 实现mit-6.5840-lab4实现过程中的思考与踩坑。

published: 2025-12-27

category: 分布式

tags: [分布式, Golang, Raft]

---

```
fault-tolerant kv service



实现一个容错的的键值存储服务





本实验分为3部分

4A，实现一个通用的replicated-state machineRSM包，利用raft实现复制指令，RSM不关心复制的指令内容



4B，基于RSM实现一个可复制的键值服务，不使用快照



4C，实现快照机制，使得raft能够丢弃日志





与lab2的关键区别是：不再是单个服务器，而是一组服务器



















4A

客户端还是通过Clerk进行交互



单服务器好实现线性一致性

复制系统中实现线性一致性则会比较复杂

则是因为：

1服务器必须对并发请求选择相同的执行顺序

2服务器必须避免基于过期状态给客户端回复

3发生失败并恢复时，服务器必须以一种保持所有已确认客户端更新的方式恢复状态







kvraft1中是骨架代码skeleton code

一个服务器必须实现rsm中定义的state-machine接口，才能使用rsm来对自身进行复制

大部分工作都是实现rsm，来提供与具体服务无关的复制机制server-agnostic replication



还需要在下一个lab中复用rsm



rsm是作为键值数据库和raft之间的一个中间层

期望的逻辑是：

服务器使用rms.submit提交指令

然后rsm中使用raft.start提交指令后进入等待

每个节点上的rsm reader goroutine从applyCh中读取请求，传递给服务的DoOp执行，指令操作

特别处理：在Leader上，reader goroutine将DoOp的返回值提交给最初提交该请求的Submit的goroutine，随后Submit将该值返回给客户端





Q：来看看StateMachine的抽象吧

A：

这是一个接口interface

DoOp

Snapshot

Restore



Q：再看看RSM的抽象：

A：

mu

me

rf，Raft接口

applyCh

maxraftstate

sm：关联上层服务



Q：回忆一波之前我的Raft设计呢

A：RaftApi是个接口，Raft是个struct，那些Leader，Commit都存储在里面的



Q：这个Op里面放一个Me和Id的需要吗？

A：



Q：事实上确实每个副本都需要read goroutine，但是submit只有Leader能正常返回

A：



Q：然而需要考虑的是Submit可能短时间内多次执行，因此每个Submit都要等待的话可能会有非常多的协程同时等待。

A：这种情况正好符合前面所说的难点：对并发请求选择相同的执行顺序，服务器必须避免基于过期状态回复客户端。



Q：因为必须要给客户端以回复，所以这里Submit后的协程必须等待？

A：似乎是这样



Q：那么现在的问题就是，如何将applyCh给出的操作精准返回给那个等待的协程？

A：有什么同步工具可用？

goroutine

channel，哈哈，笑死了都，感觉很无语，没事的

其实很简单，给个条件变量就好了，睡眠的协程确实得明确知道自己在等待那个操作就对了，所以唯一标识还得有。

read-goroutine，



Q：read goroutine在Leader和副本上的行为有所不同，但是好像没办法判断这种不同？

A；可能是这样的



Q：有一个方案可以保证read goroutine行为一致：由read goroutine取得共识的命令交给上层服务执行，但是又要考虑返回结果如何给到等待的协程了

A；等待协程可能非常多，如何精准分辨给谁呢？

这个是不是就像是操作系统里等待某个事件的处理方式？

好像是的，我想到是不是应该实现一个哈希表？用于存储阻塞的协程？

但是也是不能被什么呢阻塞呢，还是一个吗？

实现：为每个等待的协程创建一个独立的channel，确实可以这么实现，ds也是这么做的







Q：提交是有可能不返回的吧，确实是



Q；被分区的Leader不会提交一个操作



Q：为什么Submit会永久阻塞？当然一直阻塞，因为对应命令不会apply了



Q：我是要实现线性一致性吗？

A：

线性一致性的保证：一定是这样的





Q；submit需要检测的一种情况：submit后Leader切换，导致操作丢失？

A：可是这种情况要返回什么呢？而且真的不一定会丢失吧，这种情况能处理吗？



Q：是这样的，总的来说，是必须满足submit的强语义

A：

errLeader：就是说已经不是Leader了

OK，操作被丢失，永远不提交





Q：让我们来梳理一遍Submit返回的语义吧

A；

任期变化也要结束一切，这个语义其实可以呢

要有一个结束时间



核心观点：客户端应该只相信OK，其他状态一概不推断。

原来是这样子呢，





Q：暴露的错误：我之前的实现没有将快照交给上层去安装





Q：现在暴露的问题是，旧有日志的唯一标识符有问题诶，不应该，不应该有哦

A：原本是应该等待旧的log应用完的，但是现在呢，新的log已经出来，而我复用旧的log了，所以是不行的，绝对不行

看来唯一标识符还是用log_index比较好，但是这会有一个时间窗口，如果那边共识完毕这里还没把chan放入哈希表就不好了，怎么解决？

唯一标识符的问题



Q：为什么非得有个唯一标识符？

A：主要用来阻塞寻址的



opId居然应该是由客户端生成的，但是客户端重试的是同一个什么呢，或者什么

哈希确实可以解决问

UUID是唯一解了看来，原来如此呢，有意思，可以了哦，可以哦

replay重放池



还要监控raft层applyCh的close









4B

不使用快照的分布式键值服务

还有呢，并发问题

使用rsm包来复制一个服务器



Clerk并不知道哪个kvserver是Leader

如果发错了，就要重试

你的kvserver不应该直接相互通信



Q：需要注意的是Put和Get也是只有Leader上能调用的诶

A：

那么错误需要互相掩盖吗？

这里需要注意的问题是线性一致性的保证，需要所谓的全  



由谁关注wrongleader，当然是客户端，只有这个错误需要返回

Q：Put和Get的顺序如何保证

A：Put个Get顺便什么顺序，反正有版本号来保证线性一致性





Q：现在这个多机版本中的ErrMaybe是如何处理的？

A：

ErrMaybe是因为响应丢包产生的二义性，这个服务层还是处理不了，依旧只能实现至多一次





Q：现象：我只进行了一次Put请求，为什么raft层追加了那么多日志？

A：

大问题：在raft选举成功这段时间里，

put发现是ErrWrongLeader一直在重试，重试时获取state需要锁，但是选举过程也是频繁需要锁，所以这里就卡住了，等到选举成功，发现个协程都是Leader，所以start开启共识，所以就有了那么

多的appendlog请求



问题是这里不是有阻塞吗？为什么这里能重复执行这么多次？也没有开启异步执行呀，怎么就立刻返回了？

确实是有很多协程堵在了submit，但是堵在里面不应该重试这么频繁的呀，到底哪里开启了异步吗？

我不懂





奇怪了，Put仅仅调用了6次，但是Submit确在无限调用，这是为什么，中间明明没有任何代码





Q：不行，我实在是不知道是谁在不断地调用Submit，真的照不出来

A：

GetKey永久卡住了，原因居然是Get那边没有返回，所以一直在调用Submit，这么简单的事我怎么想不到呢





Q：问题是：少数派的Get也是不能完成，我确实能保证不完成呀

A：确实是





Q：难道是要实现自动切换分区寻找健康的Leader？

A：应该不是



现在是Leader一直在网络失败，无法连接，确实是要一直阻塞，但是一直不结束是什么意思



超时

网络错误都可以尝试寻找新的Leader





出现了raft层错误，真的蚌埠住了

Q：为什么lab3的测试没有出现

A：为什么lab4的测试中稳定出现？

原因是什么



Srv6是最后一条1154的log，为什么呢

  

原来是旧版的log长度有问题，可是3作为一个Leader为什么日志还会缩短呢？



现象：旧版本的Leader出现了突然日志变短的事件？这是

我不知道



Q：大型bug：原因在于收到更高term的旧Leader没有立刻结束同步日志，转为Follower角色？

A：



append中添加反向控制方法，就是这样子，修改matchIndex

matchIndex的含义是复制进度



头疼欲裂啊，

matchIndex-offset+1是起始点是为什么呢

matchIndex假设



之前发送的第一条为什么没有越界错误



有时候真的很无语，这里也没有对raft做特殊测试，旧命中了我一个大bug，而且也是概率很小的事件

导致了log突然变短，导致buildAppendEntries时候log[start]越界





4C

需要能够检测持久化对象是否过大

实现快照

合理时间是400s，cpu时间是700s，这个是合理的

主要目的是避免日志太长，重启太费时

由RSM读取快照交给上层服务执行，而不是由raft提交install方法

restore这些就由KVServer实现就好了



Q：之前好像一直没有并发控制相关内容

A：

client中的lastLeader乱了也就乱了，没什么关系

server中只有一个mapper，已经用互斥锁保护了



Q：rsm什么时候检查日志长度

A：

rsm只有一个作用，将上层服务想要共识的cmd交给raft层启动共识

有一个后台协程读取共识cmd交给上层服务

submit时检查可以

readgoroutine时检查不合适

会引起log长度改变的操作前后检查



这个机制应该是Leader和replication都得有的，而submit只是leader的，

所以还是放在readgoroutine中比较好，这样直接抓住了状态机state变化的入口，并且每台机器都会定期执行。

关键是这样能方便知道状态是哪个时刻的状态。



Q：这个日志大小监控不够直观，只有apply动起来的时候才有监控

A：如果日志一直不提交，一直追加的话，那么apply这里就不会有检查，超过阈值也不会截断

不过这也是没办法的事情，因为全都没有apply，没有应用到状态机，没有达成共识，本来就是可能无限增长的





Q：rsm与可复制语义是什么关系

A：

replicated state-machine，复制状态机，复制的不是整个服务器内容，而是增量

因为扩容这块还没讲过的



Q：index是做什么的？

A：

确实raft得知道这是哪一个时刻的快照，要丢弃哪些日志





好了，现在是log大小检测做完了

上层服务的snapshot和restore方法

apply中处理install的msg



Q：我才发现kvserver没有任何并发控制，这个原因是什么

A：

kvmapper是可读可写的，但由于线性一致性，apply是一定只能顺序应用的，所以虽然有多个op，但只有一个协程，没有任何竞态条件，所以根本不需要这个任何锁

非常完美



恢复的过程应该会重走一遍makeRSM的吧





现象：为什么CommandIndex全是1，为什么只是1呢，不可能都是1才对，Command的index是Leader负责自增的，然后底层共识后，apply到上层，这中间没有经过任何修改

确实都只concat了那么7次



出问题了，apply读取了7次就卡住了，没办法继续



简单来说leader一下子新增了很多的log，但是只有index=1达成了共识apply，然后leader

发现自己的日志过长了，apply=1的时候clip了日志，然后不知道什么原因，就只剩下leader一直尝试同步日志，同步失败了

原来果然是锁的问题，

history不是线性的了，是这样吗？可能吗

线性一致性怎么就保证不了了

有点奇怪





我的这个检查放在什么什么之后呢

有的时候日志很长，但是没有提交，因此也没办法压缩

不是现行的



怎么还有越界错误：install之后的长度可能是0？不可能啊，我全都会预留一个logentry的位置，明明做好了



rsm2有问题，然后呢，不对没问题



怎么说呢，创建的快照太慢了，创建了旧的快照，

这个原因是什么呢，apply得太慢了，这个时候底层已经安装了其他的快照，真的太慢了，反正那边在等待安装snap，现在数组已经改了，但是那边在接收旧的日志呢

旧时候的，这个顺序没有得到保证

明天梳理一下



Q：applyCh的顺序必须加以保证，raft中不止一个入口，都是因为网络请求就有可能乱序诶，这个一致性不能省略

A：确实应该保证



Q：有一个需求：当一个事件结束时，立即关闭所有的资源和协程，顺序如何考虑

A：先关闭协程，再关闭资源，因为协程可能正在访问资源，所以不能这么做。

协程发生panic后会导致整个系统panic吗？

需要有这种机制





问题是有Snapshot搅局

事实上，如果保证了单纯Cmd的顺序性，那么Snapshot不可能跑到cmd提交的后面去吧

规则：Snapshot只要大于等于nextIndex，就是可以无条件install，这是很明显的



现象：先apply了8，

然后raft发生了install，install被rsm处理前，已经完成了log的截断，所以要在snapshot中处理一下





为什么还是发生了

Q：有可能出现我的snapshot领先leader的情况吗

A：有啊，snapshot是每个replication独立调用的诶，所以可能我足够

但是呢logOffset之前的什么

确实这个时候Leader的offset可能会落后，但是可以确定的是logOffset之前的都是已经提交达成共识的



可是如果leader sync副本不成功，不能假设其进度已经追上leader了，因为这可能触发未共识log的提交，不行，绝对不行

所以只能appendEntries做文章，你直接说了什么呢



3| 4 5 6 7 8

         7 8

  

Q：关于logOffset的假设出了问题，大部分代码都得重来

A：



Q：lastApplied似乎是因为落后的snapshot强行install，确实是这样子

A：落后的snapshot强行install了



Q：遇见大问题了，发出installsnapshot的leader居然没有我的已经提交的日志？

A：

问题是prevLog可能和appendLog连不上，这是怎么回事？









线性一致性问题，执行成功和失败两种情况都解释不了诶。怎么办呢

我实现了设呢



错误确实是restore的错误

10min超时爆炸





Q：观察发现一共就分别有10个put和get操作，结果运行了10min，100%是实现有问题，那么问题在哪

A：

100多次的Leader选举

结果log有这么多次

由client请求server的getput的次数也不多

最后一个rsm的apply止步于1w行左右

简单来说日志没有无谓增加太多

原来是没有处理返回结果为空串，导致client一直轮循



这个方向不是主流，主流是这样的吧，模型什么呢



4c前3个都pass了，说明问题应该不是很大的



目前看来，crash和partition的支持都不好

recover一直过不了





新添raft的字段要考虑恢复时如何维护其状态呀





Q：观察到所有crash之后，putversion0被执行了

A：我恢复的时候难道没有读取snapshot吗？读了呀，那为什么找不到这个key会到version=0创建中？

应该不允许才对，snapshot有这个键呀

raft恢复-rsm.sm.store恢复上层服务的键值的诶





Q：我看日志create key都只执行了一次，怎么可能出现验证器的情况？









Q：版本从1开始，怎么可能有0啊，这个确实得看看

A：

这种bug真的很逆天。

我tm又一个decode中的逻辑写错了，导致restore反序列化全为0











Q：多个客户端时的recover有问题？

A

15个就随便通过，20怎么也pass不了





Q：目前的问题：manyclients太多的时候过不去

randomkey，多key共识的时候过不去（偶尔过53s，多数时候不过去）

这个120s没跑完就超时了？

随机性实在太强，这个，真的做不了



gpt原因：

1submit在snapshot或restart后永久阻塞？

测试不报错，就是不结束





耗时315s通过最难测试用例，但是为什么



原因是选举太慢了吗？

剩下的bug就是log剪切的问题了，为什么会这么幸运呢

这个日志应该是









Q：manyclients的问题

A：

只有2次选举，

只是会宕机罢了

这个没有什么阻碍，为什么运行这么慢？

为什么这里有40个客户端？不是说好了20个吗？



测试：每个客户端一分钟内不断put，能进行两三百次，有的七八百



20个clients，跑完一个iteration需要60万行日志，210s，足足3分半，看看15个





clients

15：每个it2.8w

16：每个it1.6w-3w

17：每个it1.5w-2.6w

18：每个it2.2w左右

19：每个it1.3w-2.5w左右

20：





通过统计观察：

15个nclients时GET请求只有4-500个

20个nclients时GET请求有7w个，为什么差距这么大，为什么GET请求这么多

PUT请求也就2-3倍，get请求数量是140倍



Q：居然还有这种bug：现在的问题是1号的任期是如何变成6的？明明没有进行选举，

A：问题还是1号没有立刻转为Follower这种极小概率触发的bug呀。真的厉害了。



Q：现象：成为Leader之后clear都失败了







pass4A：100s

pass4B：218s

pass4C：171s



4C：通过测试耗时312s

但是有可能永远无法达到



4B最后一个测试用例还是不好过，为什么关闭snapshot反而不好过呢，很奇怪



为什么



现在观察到了：确实是某些请求一直没有返回，所以导致一直重试

那么这是什么情况呢，为什么snapshot就能避免呢？





有一定概率响应丢包，或者说返回得非常晚非常慢，一个客户端会进行并发吗？这个很难说的吧

一直卡在server端口





Q：那么问题是快照为什么能够避免丢这个包？

A：原因是什么



Q：

get和put最长可达70多s，为什么这么多s

大部分时间其实还是什么呢，就是啊，这里确实要特殊处理吗？

重试哪一个？



很多不是put了一千多次，而是因为一直阻塞着没办法才一直重试，重试到什么时候呢



70多s可以允许超时重试700多次，但是为什么要700多次才得到结果？

Leader切换得其实不是很频繁，一个查询达成共识为什么需要这么久呢，不知道





Q：确实是共识出问题了，这是怎么

A：

一共共识了130多个cmd

提交了620多个cmd

某些client上万次调用，但是根本没有提交命令

最多的什么呢

差点被骗，依旧不是问题



这确实是一个高并发场景，WrongLeader



问题：最后一个Leader是466284，最后一个commitforward是9272行

确实是共识出问题了

最后一个cmdstart是



最后一个leader：1/T128



466284，最后一个commitforward是9272行

469951行，最后一个Leader保有这么多的日志，没有丢失，但是为什么共识这么慢呢？

呢还是允许提交了



然后appendlog：

srv4 成功

srv6 成功



可能性：分区脑裂，小分区的leader一直不承认自己不再是leader，所以client就没有去尝试其他的





我花了535s通过了全部raft的全部测试用例，这里还是没问题





Q：我想到一个问题可以解释所有目前现象

1raft层测试完美通过

24c带快照完美通过，不带快照就难以通过

3一定概率触发

答案就是当leader远远领先follower时探测太慢

探测太慢->迟迟不共识->client不断start请求->继续在leader上appendlog->leader更加领先->更加难以探测follower的进度了



A：看日志确实是这样，最后也是完美解决了问题



Q：不过问题没哟完全解决

A：有可能存在问题就是对于一个在小分区被旧leader大量灌输log的情况下，新leader线性探测复制进度的效率还是不高，不过这些都是很极端的情况





最后是

4A使用54s通过

4B使用143s通过

4C使用127s通过



总共324s，比参考速度还快了这么多





新bug：全都是leader
```

Golang相关

```
debug经验

Q：第一个错误：submit没有返回是吗？
确实有可能提交的命令可能是无法返回的，也可能被覆盖的，这个时候



golang
Q：golang中的类型断言
A：
这里类型断言怎么用的
args作为请求时都是any序列化之后的事情，在raft层之间是如何反序列化成为正确的类型的？
原来如此吗？这里要靠register，所以字节流形式应该有类型信息
这方面没什么好说的
```
