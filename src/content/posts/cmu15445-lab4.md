
---

title: cmu15445-lab3分析与实现

description: 实现cmu15445-lab2实现过程中的思考与踩坑。

published: 2025-10-20

category: 数据库

tags: [数据库, Cpp]

---

```
并发控制



实现并发乐观多版本控制，为数据库添加事务支持



冲突

读写冲突导致不可重复读

写读冲突导致脏读

写写冲突导致丢失更新



任务1：时间戳管理

任务2.1：存储格式

任务2.2：顺序扫描/元组检索

任务3：MVCC执行器

任务4：主键索引



加分任务：事务中止，可序列化验证



多版本并发控制支持，无需修改表堆

mvcc的高级版本duckdb的应用

与增量表架构类似，对于每个元组，额外存储元组增量，成为undolog



不依赖项目3是为什么



事务状态有进行中，已提交，已中止和受污染的

隔离级别：读未提交，可串行化，快照隔离

cpp为泛型



task1

为事务分配到达时间戳和提交时间戳，直接使用原子自增类

水位线机制，记录系统中还在运行和中止的最低的时间戳

测试在TxnTimestampTest

事务启动提交和中止都需要更新水位线

看测试的意图吧

时间戳的意图：

abort之后水位线增加了。

如果没有活跃时间戳，那么，水位线等于最新的提交时间戳

这里说的logn的数据结构应该是一个有序表，查找后继的接口支持



task2

元组的重构

顺序扫描

事务数据存储在三个位置，表堆，事务管理器以及事务的内部工作空间。

表堆存储最新的元组数据，事务管理器为每个元组存储一个指向最新的撤销日志的指针，事务则存储自己的撤销日志

读取元组：从最新版本逐步撤销

一些工具类

不安全地修改元组是什么意思

事务头文件，每个事务都存储向量，包含其修改过的所有元组的增量记录。UndoLog是以元组为单位存储的

调用ModifyUndolog和AppendUndolog来追加新的日志。

使用UndoLink把单个事务所有的Undolog连接在一起

通过Getundolog直接获取某个事务的第i个UndoLog



元组重构的辅助函数，从一个元组模式，基础元组和元数据和一个撤销日志列表，重构元组

is_deleted的和撤销日志中的is_deleted不总是相等。可能最新的什

重构元组不需要依赖任何其他参数

重构元组逻辑由调用者决定将哪些版本链放入重构逻辑中



Q：理清这个is_deleted字段的含义。

A：一个字段是否删除的含义。

假设中间有一个版本是is_d的

1回退到这个版本之后的版本，正常重构

2回退到这个版本之前的版本，直接越过is_d，继续重构

3回到到刚好这个版本，那么这个元组就没必要重构了，因为当前事务应该看不见它才对

拷贝模式有在构造方法中重构列索引，无需惧怕

确实返回值也是这么设计的



Q：为什么传递的最新版本元组要求const

A：每一个版本就涉及到拷贝吧。



重写顺序扫描执行器

1获取该元组在事务读时间戳之前的所有撤销日志

2重构出过去版本的元组

3输出该历史版本的元组



Q：删除的撤销日志为什么出现在版本链的中间？

A：两个事务同时读取，一个更新，一个删除，删除在前。

查

增加：直接插入就完了，标上一个未提交

删除：标记删除就完了，标上一个未提交

更新：提取被修改的字段，构造一个unlog，放入版本链，标上一个未提交。

扫描表时候的三种情况：

所有事务对元组的增删改

1已经是需要的元组版本。

2另一个未提交事务的元组或者其版本较新。

3元组是当前事务的修改。



Q：为什么需要额外支持未提交的元组时间戳

A：一个未提交事务的提交时间戳是未知的，使用一个临时时间戳，说明很大，简单比较一下



Q：未提交的版本肯定不可见，但是为什么不直接使用创造这个版本的读时间戳？

A：是我想错了，读时间戳可能比较大也可能比较小

10  11  12

15  16  17



Q：为什么未提交就默认不可见？

A：有没有可能它提交后应该在我可见范围？不可能，本事务已经分配时间戳了，这个元组只有可能是在后面创建的，应该视作未来不可见。

只看在读时间戳之前提交的事务。



Q：如何标识未提交？

A：为什么要通过这种方式标记？其实都一样

时间戳是有符号的整数。破坏了比较逻辑的语义，只需要比较一次吧。

用负数标识需要比较两次，用极大的未来时间戳标识只需要比较一次



但是扫描的时候还是要比较一次当前事务的修改者是否是自己



Q：不仅需要标识未提交事务，还需要标识是哪个事务的未提交事务，但是呢事务不是有自己的工作集吗

A：事务如果更新元组，更新元组的上个版本还是放在撤销日志版本链里里面的吧。











事务信息结构

当前事务维护一个undolog数组

事务支持行为

一个更新undolog：

事务需要支持什么呢别的事务获取当前版本的的某个插槽。

查询信息

修改撤销日志

追加撤销日志

添加锁

添加扫描谓词

获取扫描谓词

获取撤销日志



事务管理器信息结构

事务管理器支持行为





通过工具函数修改版本链

事务管理器的实现内容怎么说

1.同时修改元组和撤销日志

2.同时获取元组和撤销日志

3.查询某个元组的撤销日志

更新元组如何保证原子性





事务支持的顺序扫描器逻辑：

知道当前页面，获取对应插槽的undolog版本链



Q：事务修改了元组把撤销日志保留在工作区，那么什么时候将所有撤销日志刷到硬盘呢？

A：问题还有就是什么



当前事务读取一个元组

当前元组可能是未提交可能是已提交，已提交肯定当前读时间戳小于或等于。

所有时间戳更大的撤销日志都要应用上



Q：这个测试有点坑，被删除了还非得返回非空，然后重构元组的时候白干

A：函数注释就有问题。



task3

STW时间，垃圾回收

各种删除插入执行器。

第一个insert执行器改为事务支持版

Q：需要考虑什么问题？

A：插入要原子的插入。

插入：修改当前页的tuples计数器，插入一个tupleMeta，从页尾往前填充给元组数据，这些都需要原子地完成。

因此需要一个页面锁，这个容易支持

需要在事务管理器中维护版本链指针，这个也要原子地完成。所以这些结构都有锁。

检查函数检查的是指针，事务号以及索引。





Q：为什么insert时需要维护版本链信息？

A：因为删除的时候是软删。不对，我不可能去检查哪个元组是软删的



总而言之，插入的时候直接插入就行了，维护一下写集，不管版本链。



一次只允许一个事务执行提交函数。



思考事务的写操作有什么？

新增：没有undolog版本链

删除：新增一个删除的undolog，没有任何修改，原元组软删标志位设置为1

更新：新增一个更新的undolog，这个undolog存储当前修改的字段



思考：提交函数应该做什么？

1.获取提交时间戳，将undolog版本链中的未提交时间戳全部刷成提交时间戳

2.将写集中插入的所有记录时间戳刷成当前时间戳



更新和删除的基础元组也要刷成当前的提交时间戳吗？



原来有交互式的shell，使用浏览器



Q：这个顺序不太好，应该先实现所有的generateundolog函数最后实现事务提交吧

A：应该这样的



更新逻辑：

如果一个元组正在被一个未提交的事务修改，则不允许其他事务的修改，这种情况是写写冲突。

确实是这样，如果另一个元组修改了，插入一个新的undolog，随时可能两个事务都提交，这样就是后来的事务没看见已提交的事务了。

两个事务删除同一个元组也会导致这个。

检测到写写冲突会抛出异常。

需要实现更新为流水线中断器，缓存当前的所有元组才对。



这个并发问题难得要命呀

创建一个全新的undo_log，更新和删除的情况

这个基础元组永远是最新的情况诶。



现在我要问了，确实只是创建一个undolog维护上一个版本而已。

Q：写写冲突有什么必要直接停止？





Q：更新执行器为什么要实现为中断器？

A：为了避免死锁？还是什么风险？其实好像没有什么问题，但是我的index迭代器有加锁，所有不好实现。最好还是缓存一下                                                                                                                                                                                                                                                                                                                             



Q：这个删除冲突是如何出现的，为什么这个要求是冲突呢？

A：不符合串行化顺序要求，事务基于过时信息做出的决策，可能破坏业务逻辑

                          

Q：为什么undolog不包含任何临时事务的时间戳？

A：因为那样导致的是写写冲突



Q：如何检查写写冲突？

A：时间戳，如果大于当前读时间戳且不等于当前事务的临时时间戳，要么是别的事务在修改，要么是别的事务修改完毕提交了。                          

反正都不满足可序列化执行。不允许。



Q：如何保证元组的原子修改的？事务管理器提供的一个工具函数的具体逻辑

A：元组页面加锁。

通过写写冲突检查逻辑

如果确实需要修改，调用加锁的不安全原地修改

不安全原地修改逻辑抽取是因为要支持同时原子修改元组和维护undolog版本链，要在另一个函数中加锁

然后更新undolog版本链信息。

当前记录的页面被锁定，那么有什么逻辑要直接修改undolog版本链吗？应该是没有。所以是原子的。

还有原子地获取元组以及undolog版本链。





Q：更新逻辑。

A：

检查记录是否有当前的undolog版本链。

1.有则叠加修改

2.没有则创建

但是如何保证原子性。



场景：

加锁检查，发现没有，释放锁。

另一个事务检查没有，并且立刻创建了一个undolog，插入。

然后当前事务会在更新元组的时候检查，发现写写冲突，直接失败？











Q：为什么要原子的获取元组和undolog？

没有必要。

base的时间戳是本事务，叠加修改；获取第一个undolog，然后修改

不是本事务，尝试依据base_tuple创建一个undolog，然后更新元组的时候可能发现有其他事务修改

但是创建更新undolog时，需要获取undolink，这个时候如果不原子获取link的话版本不一致，原子读的价值就在这里

但是这个写写冲突的发现应该能够提前吧。

读时间戳是最后一个





Q：检查函数究竟需要检查什么，不是只需要检查一下时间戳吗。为什么要那么多参数

A：应该只是需要检查时间戳，这有点像是双重检查锁



Q：index_scan也许需要修改，因为会受到自更新的影响。但是怎么修改？表堆不会删除，但是索引会有删除的情况。

A：一次性收集玩就行，但是这样流水线处理的目的又是什么呢。火山模型的优势全没了。

    

Q：提交，为什么只允许一个事务提交

A：遍历每个元组，刷新时间戳为提交时间戳，通过写集

写集是rids，undolog存储的是老版本的状态，也是老版本的时间戳，不需要修改，只需要修改基础元组的时间戳。



这个表堆迭代器依赖的就是页面的指针，每次下一步都是重新获取页面锁。



不开启时间戳就是隐事务。



Q；为什么不允许未提交的undolog。

A：因为这是典型的写写冲突，可能导致丢失更新。基于过时的快照决策了。



一些bug：

undolog中的元组模式

更新已有的undolink



关于为什么捕捉不到错误，因为测试框架的目的就是运行所有测试用例，肯定不是主线程去运行测试代码的



原来使用的是cpp异常机制而不是多个线程

undolink可能并不存在，因为可能是由本事务插入的

抛出异常也算内存泄露吗



插入过程还可能发生在tombstone，墓碑上面，确实，这个必须支持

删除之后再插入，这个确实得支持。

更新一大波之后打算删除它，这个肯定得支持。

在自己创造的墓碑上插入新的内容还没支持。



开始垃圾回收

事务加入映射表就永不删除。



水位线机制用于判断哪些事务可以回收，某些事务的undolog不包含任何对最低时间戳可见的撤销日志。

水位线以下的事务全部删除，连带着他们的undolog。

依据事务的写集遍历表堆还是undolog？



undolog的时间戳全是事务提交时间戳，但是如果没有更新的事务覆盖这个版本，那么对水位线还是可见的。

所以要通过写集遍历表堆。在此之前先遍历事务。

问题是遍历哪些事务？需要事务的提交时间戳

小于水位线：有可能

等于水位线：也有可能这个事务的版本链

有没有办法记录最低没有被回收掉的事务？应该没有，有可能一个事务的记录一直没被覆盖，一直能看见。

即使维护了，从min遍历到watermark还不是遍历哈希表呢



对于哪些中止的事务怎么办？中止的事务是没有提交时间戳的，他们修改过的元组也是打上的他们的时间戳，并且还没处理。

中止的事务的undolog如何处理？需要立即处理，因为这样别的事务都修改不了诶。

中止的话就当这个事务提交了吗？

如果事务遇见了写写冲突，污染-》中止，应该通过写集把应用所有的undolog，撤销为上一个版本。

然后这个中止的事务就该回收了。我认为是这样的。确实是这样的。

事务回滚



垃圾回收，可能导致一些undolink指针失效。



所有初始化内容都是事务0插入的。



这个什么沟八的5号事务没有写集吗？我cnm

我的思维误区，其实刷新的时候刷新的是写集，也就是基础元组，而不是所谓的undo_log。

要检查的是可见版本是否会经过吧。

可见版本，垃圾回收的时候检查的这些事务一定有可见版本吗？

提交时间戳等于水位线的也可能要回收。因为使用不到undolog

water>=txn的提交时间戳>undolog版本链的时间戳，因此



这个水位线测试有点问题吧。



提交时间戳有什么必要原子类。



原来是因为所有没有undolog版本链的事务也可以安全回收。









task4

就是并发的同时维护主键索引之类的东西，

索引的扫描，索引的更新

怎么感觉有点无聊啊，这个lab，我没看见要求我实现可串行化

附加任务是中止事务回滚

一个是可串行化验证





主键索引

插入主键索引时如果重复，中止事务

先检查索引中是否存在对应键，然后才在表堆中插入元组，然后插入索引。

污染的事务也会导致其他事务与之发生写写冲突。

对呀，between语句还没有支持呢。

会屏蔽顺序扫描优化为索引扫描。



Q：为什么主键索引要这么维护？先插入表堆再插入主键索引有什么问题？

A：

1事务T1T2先后插入相同主键的元组在表堆，而且也不知道有没有相同主键在表堆，再去插入主键索引，可能一个事务成功，可能都失败。

污染后虽然元组会被回收，但是会产生很多的垃圾，所以有必要先检查以下主键索引。

2事务T1T2先后检查主键索引，发现不存在，尝试插入表堆，再插入主键索引，只有一个能成功，第二个失败被中止。后续事务检查主键

索引都会发现冲突，然后标记为污染后中止。

看来确实是方案2更好。



这个主键索引可以使用不支持重复键的索引，这样检查时就顺便借助位锁插入占位符了



Q：我完全没看见，看来这个主键列信息在表的哪里维护？

A：有主键的时候会创建主键索引，索引的元信息有主键列。



索引模式有列名，执行计划模式没有列名



为delete、update和mvcc索引扫描执行器添加索引支持。

整个系统信息存在于表堆和索引上。表堆上确实是有多版本支持。

但是索引好像还没有诶。

当前写版本替换了老版本，虽然对当前写事务可以用索引，可是旧的事务应该还应该通过索引扫描到旧版本呀。

但是旧版本的rid还是不变，可以不将对应键从索引中删除

怎么都在扯插入执行器在墓碑这里做什么呢？删除执行器不删了还不行吗？







Q：事务中的undologs如何受到并发保护的呢？

A；undolog版本链只有追加和修改的可能性以及读取的可能性。

事务T1查询旧版本通过一个正在修改的事务T2的undolog撤销时，有没有可能出错？基础元组和需要的undolog对不上？

答案是不可能，修改基础元组和undolog是是在一个原子操作中完成的，那就没问题了





删除操作和所谓的什么呢



当我向墓碑插入一个元组时，应该使用更新，那么来看看插入和更新的区别是什么吧

主要什么呢，原来如此，所以主键索引也是不允许这样的吗？

索引支持。



主键索引这么做一直不删除

所以插入时检查主键所以的时候要看是不是删除的，若删除则使用更新，若未删除则使用更新

若删除时打算更新，如何保证原子性呢，一样使用一个检查函数



如果是二级索引呢，怎么找得墓碑

这种方法也不完全行，因为找不玩墓碑。



那么可重复的二级索引如何维护呢。



二级索引一次可能查出来多个，插入时可以检查哪一个是删除的，如果有删除的，那就把

二级索引清理吗？

二级索引，可能有一个rid



二级索引的清理逻辑：

索引读取时：扫描扫出来可能是当前事务不可见的，可能是当前事务

watermark，如果当前元素已删除了呢，



Q：如何判断哪些二级索引可清理？

A：读出来发现什么呢。

获取水位线，看看那个版本是否已经是水位线都不可见的。水位线不可见吗？STOP the world

怎么检查效率最高呢？

惰性检查：现在我不知道对索引上的值究竟是哪一个版本，看这个版本是否低于水位线能看见的版本。如果能低于则可以删除

但是删除b+树上的哪一个呢？有很多个呢，不能直接反向索引叶子节点的插槽诶。这个设计可能还是有点问题。

所以二级索引就先不处理了吧



如果要插入的主键被未来的事务删除，我是否可以认为这是一个空槽？

已经删除，这个也会是一个错误



GenerateNewUndolog两种nullptr的不同结果：

1.目标是空，打算全部删除，那么旧版本肯定需要全部保留，并且delete是false

2.基础元组已经删除，传递的是nullptr，现在要修改的话，基础元组的旧版本要保留，但是什么字段也不存储，因为这个时刻的状态就是

没有字段。



更新执行器其实并不困难，就是旧的索引不删除，就是了，如果有什么呢，不过我得保证不要重复。

对应字段被更新了，但是没有从索引中删除的话

对应的索引键肯定不能删。

如果没有更新对应字段：什么都不管

如果更新了对应字段：新插入一个索引键



也就是说这个索引扫描出来的记录可能是已经过期的了。

所以要检查重构一下看看是否符合预期，如果不符合，那就没办法了呢。原来如此



因为主键是唯一的

一旦插入，位置一直不变

但是如果是修改了主键呢？

修改主键，还是有一个以前的索引键在那里。

插入的一个新的索引键是否需要改变位置？

一个元组就算有多个主键版本，就有多少个索引条目指向这里。但是总得有删除的情况吧？

删除的情况就是：当前事务满足删除的条件，给他删除了。

这个墓碑元组的复用呀，如何保证系统的状态一致性还真困难呢。

有可能更新的时候，原主键可能已经变了，但是索引中还保留着，但是其实已经没有这个主键了吧，这个时候应该允许插入才对。

可是呢，一个插槽在没有删除的情况下刚好。



插入数据不仅仅是添加一行，涉及到复杂的冲突检测和版本管理呢。



主键更新是不常见的

还有主键更新一个，那么旧的就是被覆盖了

一个元组先插入了1，然后更新为2，然后当前任务插入，再来一个事务插入1的元组，这个时候怎么办呢，1跟2没什么关系吧，怎么

插入呢？



这个tombstone复用只有删除的时候才允许，但是如果没有删除的话，只能插入一个新的元组了。一旦创建虽然可能存在



我觉得这个时候应该额外新增条目吧，只是索引可能有两个了，真的特别恶心哦。



核心目标：

1.保证旧事务也能通过索引扫描出旧版本

2.索引条目不删除



1推出2

一个元组一旦插入索引就不删除，因为可能有事务看见这个版本。

但是元组还会支持更新，索引指向不一定就是有效的，只能说曾经有一个版本出现在这个元组。

主键的唯一性约束怎么看呢

旧的版本有什么呢

插入主键的条件：当前事务的读取时刻不存在对应的元组



如何支持更新呢？

更新应该设计为元组迁移

更新应该分为更新主键

以及不更新主键。

更新主键的话吗？



更新的时候主键索引需要特别关注。





工业数据库的死元组复用怎么解决呢？

postgre使用的是一个FreeSpaceMap，记录哪些死亡元组空间可以复用



没有必要害怕什么主键不一样



总感觉不能这么复杂，迁移就迁移吧，只要改变目标的rid不久行了吗



搞懂这个元组迁移的语义，怎么



我认为我这个重构元组的逻辑还是有问题



GenerateNewUndolog

对于一个已删除元组进行更新，之前有这种情况吗？

自然情况下应该不会出现这种情况吧。因为扫描都扫描不出来。



如果一个元组是被我删除的

那么undolog里有上一个版本的全部信息吧应该的



理解删除在undolog版本链中的概念

重构元组确实需要修改内容











为什么这里有个数据库实例的动态链接库呢









原来这个实验四的mvcc是上海交大的张驰基于tum德国慕尼黑工业大学的hyper实现的，

是一种occ控制方法，区别于innodb的悲观锁协议，解决写写冲突的时候加行级锁

迟策居然是上海交大计算机系第一名









debugs

一个是lambda表达式没有调用



错误是被我删除了什么，



主键索引的更新方式，如果是主键。昨天什么的呀，不可见的变可见的了



旧的版本是删除的，那么现在新的版本要更新吗

现在本事务要删除自己的更新，原本有一个undolog记录上一个版本内容，上一个版本内容如果是未删除的

那么删除就是把基础元组所有内容拷贝到旧版本

但是如果上个版本也已删除，那就不用管了





有一个错误就是我的更新墓碑的时候没有把删除标志位丢掉



要求没有主键更新的时候只能有一个undolog





bonus

一个实现回滚

方案1：恢复基础元组，但是撤销日志保留在undolog版本链中

方案2：一个是原子更新，改变link的指向
```

Cpp17相关

```
cpp tips：
Q：cpp如何为某个泛型方法单独指定类型
A：通过模板特化

更新一个已删除的元组吗哈哈，有点意思呢，就是这样子，真的很烦

```
